import { ExamLi, ExamPoint, Note, Table } from "../../ui";

파괴적 작업에 MFA 코드를 요구해 실수·악의적 삭제를 막습니다.

<Table
  head={["구분", "내용"]}
  rows={[
    [
      "MFA 필요",
      <>
        객체 버전 영구 삭제 · <b>버전 관리 상태 변경</b> — 중단(Suspend)뿐 아니라 중단 후 재활성화도
        포함
      </>,
    ],
    ["MFA 불필요", "삭제 마커 추가(일반 삭제) · 버전 목록 조회"],
    [
      "전제·설정",
      <>
        버전 관리 활성화 버킷 · <b>루트 계정만</b> 활성/비활성 가능 · 콘솔이 아닌{" "}
        <b>CLI/SDK/API로만</b> 설정
      </>,
    ],
  ]}
/>

<Note>“버전 관리를 켜는 데는 MFA가 필요 없다”는 MFA Delete가 아직 꺼져 있을 때의 이야기입니다. 일단 MFA Delete가 켜진 버킷에서는 버전 관리를 중단하는 것도, 중단했다가 다시 켜는 것도 MFA 코드를 요구합니다.</Note>

<ExamPoint>
  <ExamLi>“MFA Delete를 활성화하려면?” → 루트 사용자 + CLI/API + 버전 관리 ON. 콘솔 설정 불가 함정 주의.</ExamLi>
</ExamPoint>
