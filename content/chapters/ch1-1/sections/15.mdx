import { WarnBox } from "../figs";

감사 목적으로 버킷에 대한 <b>모든 요청(허용·거부 불문)</b>을 다른 S3 버킷에 기록합니다. Athena 등으로 분석할 수 있으며, 로깅 대상 버킷과 로깅 버킷은 **같은 리전·같은 계정** 이어야 합니다.

<WarnBox>로깅 버킷을 모니터링 대상 버킷과 동일하게 설정 금지 — 로그가 로그를 낳는 <b>무한 루프</b>로 스토리지 요금이 폭증한다. 반드시 별도 버킷 사용.</WarnBox>
