import type { SelfQuizEntry } from "../../schema";

/**
 * ch1-1 섹션 셀프 퀴즈 (이슈 #106) — 인출 카드 아래 자기채점 덱.
 * 문항 지침(#98): 짧은 시나리오/사실 큐 + 판정 가능한 1~2문장 정답. 소재는 각 섹션
 * EXAM POINT가 1차 원천. 카드 = 서술·정교화, 셀프 퀴즈 = 핵심 사실 판정.
 *
 * ch1-1 인출 카드(session.ts)는 #100에서 별도 작성 예정 — 셀프퀴즈가 먼저 착지하므로
 * #100 카드 작성 시 이 문항들과 문장 재탕 금지 규칙을 지켜야 한다 (#100에 코멘트 전달).
 */
export const selfQuiz: SelfQuizEntry[] = [
  // ── 01 S3 개요 — 버킷·객체·Key ────────────────────────────────────────
  {
    section: "01",
    q: "500GB 백업 파일을 단일 PUT 한 번으로 S3에 올린다 — 성립하나?",
    a: "성립하지 않는다 — 단일 PUT은 최대 5GB, 초과는 멀티파트 업로드 필수(100MB 이상부터 권장)다. 객체 자체 한도는 50TB(멀티파트).",
  },
  {
    section: "01",
    q: "콘솔에서 “폴더”를 만들어 파일을 넣었다 — S3에 디렉터리 구조가 생긴 건가?",
    a: "아니다 — S3에 폴더는 실재하지 않고 UI가 Key의 prefix를 폴더처럼 보여줄 뿐이다. 객체 Key는 “folder1/file.txt” 같은 전체 경로다.",
  },
  {
    section: "01",
    q: "다른 회사 계정이 이미 쓰고 있는 이름으로 내 계정에 버킷을 만들 수 있나?",
    a: "불가 — 기본 글로벌 네임스페이스에서 버킷 이름은 모든 계정을 통틀어 전역 고유다(2026 신설 account regional namespace만 예외 — 시험 기준은 글로벌). 이름은 글로벌이지만 버킷 자체는 특정 리전에 생성되는 리전 리소스다.",
  },

  // ── 02 S3 보안 · 버킷 정책 ────────────────────────────────────────────
  {
    section: "02",
    q: "버킷 정책으로 퍼블릭 읽기를 허용했는데도 여전히 접근이 거부된다 — 1순위로 확인할 것은?",
    a: "Block Public Access — 4개 설정 중 정책 관련 설정(BlockPublicPolicy·RestrictPublicBuckets)이 켜져 있으면 버킷 정책이 허용해도 공개가 차단된다. 통상 계정 수준에서 4개를 모두 켠다.",
  },
  {
    section: "02",
    q: "다른 AWS 계정의 IAM 사용자에게 내 버킷 접근을 허용해야 한다 — 시험 정답 수단은?",
    a: "버킷 정책의 Principal에 상대 계정을 지정한다 — 단 이것은 리소스 쪽 절반이고, 상대 계정에서도 IAM 정책으로 해당 S3 액션을 허용해야 접근이 성립한다(양쪽 Allow 필요).",
  },
  {
    section: "02",
    q: "같은 계정에서 IAM 정책은 Allow, 버킷 정책에는 명시적 Deny가 있다 — 접근 결과는?",
    a: "거부 — 평가 규칙은 “(IAM 허용 OR 버킷 정책 허용) AND 명시적 Deny 없음”이라, Deny가 하나라도 있으면 무조건 거부된다.",
  },

  // ── 03 S3 정적 웹사이트 ───────────────────────────────────────────────
  {
    section: "03",
    q: "S3 정적 웹사이트가 403 Forbidden을 반환한다 — 확인할 두 가지는?",
    a: "① Block Public Access 해제(계정/버킷) ② 버킷 정책으로 s3:GetObject를 “*”에 허용 — 콘텐츠 전체가 퍼블릭 읽기 가능해야 서비스된다.",
  },

  // ── 04 S3 버전 관리 ───────────────────────────────────────────────────
  {
    section: "04",
    q: "버전 관리 버킷에서 (버전 지정 없이) 객체를 삭제했다 — 실제로 일어나는 일과 복구 방법은?",
    a: "실제 삭제가 아니라 삭제 마커가 추가될 뿐이다 — 마커를 지우면 객체가 복원된다. 특정 버전 ID를 지정한 삭제만 영구 삭제다.",
  },
  {
    section: "04",
    q: "한번 활성화한 버전 관리를 완전히 끈(unversioned) 상태로 되돌릴 수 있나? 활성화 전 객체의 버전 ID는?",
    a: "되돌릴 수 없다 — Suspend만 가능하고 기존 버전은 유지된다. 활성화 이전 객체의 버전 ID는 null이다.",
  },

  // ── 05 S3 복제 (CRR / SRR) ────────────────────────────────────────────
  {
    section: "05",
    q: "CRR을 켰는데 원래 있던 객체들이 대상 버킷에 안 보인다 — 오류인가? 해법은?",
    a: "정상이다 — 복제는 활성화 이후 새 객체만 대상이다. 기존 객체·복제 실패분은 S3 Batch Replication으로 복제한다.",
  },
  {
    section: "05",
    q: "버킷 1→2, 2→3으로 복제를 설정했다 — 1에 올린 객체가 3까지 전파되나? 복제의 전제 조건은?",
    a: "전파되지 않는다 — 복제는 체이닝 불가다. 전제는 원본·대상 버킷 모두 버전 관리 활성화 + S3에 부여하는 IAM 역할.",
  },

  // ── 06 S3 스토리지 클래스 ─────────────────────────────────────────────
  {
    section: "06",
    q: "“내구성 11-nine이 필요하니 Standard를 골라야 한다” — 이 판단이 왜 틀렸나?",
    a: "내구성은 모든 클래스에서 11-nine으로 동일하다 — 클래스를 가르는 축은 가용성·검색 시간·최소 저장 기간·비용이다.",
  },
  {
    section: "06",
    q: "유실돼도 재생성 가능한 썸네일 사본을 저비용으로 보관하되 즉시 접근은 유지 — 정답 클래스는?",
    a: "One Zone-IA — 단일 AZ라 AZ 파괴 시 유실되지만, 재생성 가능한 사본·2차 백업이 조건일 때의 저비용 정답이다.",
  },
  {
    section: "06",
    q: "규정상 7년 보관, 조회는 거의 없고 꺼낼 때 반나절 기다려도 된다 — 정답 클래스는?",
    a: "Glacier Deep Archive — 최저가, 표준 검색 12시간(대량 48시간), 최소 저장 기간 180일. 신속 검색은 미지원이다.",
  },

  // ── 07 수명 주기 규칙 + S3 Analytics ──────────────────────────────────
  {
    section: "07",
    q: "실패한 멀티파트 업로드 조각들이 스토리지 비용을 계속 발생시킨다 — 정답 해법은?",
    a: "수명 주기 규칙의 미완료 멀티파트 업로드 정리(AbortIncompleteMultipartUpload)로 자동 삭제한다.",
  },
  {
    section: "07",
    q: "S3 Analytics 리포트를 근거로 Glacier 전환 시점을 정하려 한다 — 성립하나?",
    a: "성립하지 않는다 — S3 Analytics는 Standard → Standard-IA 전환 추천만 제공한다(One Zone-IA·Glacier 미지원).",
  },

  // ── 08 S3 이벤트 알림 ─────────────────────────────────────────────────
  {
    section: "08",
    q: "S3 → SQS 이벤트 알림이 안 온다. S3에 IAM 역할도 부여했다 — 무엇이 잘못됐나?",
    a: "권한 방식이 틀렸다 — S3가 대상에 게시하려면 IAM 역할이 아니라 대상 측 리소스 정책(SQS Access Policy 등)에서 S3를 허용해야 한다.",
  },
  {
    section: "08",
    q: "S3 이벤트를 Step Functions로 보내고 이벤트 재생(Replay)도 필요하다 — 정답 통로는?",
    a: "EventBridge 통합 — 고급 JSON 필터링, 18개 이상 서비스로 전달, 아카이브·재생을 지원한다. SQS FIFO도 직접 대상이 안 되므로 EventBridge를 경유한다.",
  },

  // ── 09 S3 퍼포먼스 ────────────────────────────────────────────────────
  {
    section: "09",
    q: "단일 prefix에서 초당 GET 10,000건이 필요하다 — 기준 성능과 해법은?",
    a: "기준은 prefix당 초당 3,500 PUT/COPY/POST/DELETE · 5,500 GET/HEAD — prefix 수는 무제한이므로 여러 prefix에 분산해 확장한다.",
  },
  {
    section: "09",
    q: "지구 반대편 사용자들의 대용량 업로드가 느리다 — 정답 조합은?",
    a: "Transfer Acceleration(가까운 엣지 경유 → AWS 백본) + 멀티파트 업로드 병행 — 업로드·다운로드 모두 가속된다.",
  },
  {
    section: "09",
    q: "대용량 파일 다운로드를 병렬화하고, 파일 앞부분(헤더)만 먼저 읽고 싶다 — 정답 기법은?",
    a: "Byte-Range Fetch — 특정 바이트 범위를 병렬 GET해 다운로드를 가속하고, 실패한 범위만 재시도하며, 부분 조회도 가능하다.",
  },

  // ── 10 객체 태그 & 메타데이터 ─────────────────────────────────────────
  {
    section: "10",
    q: "“태그가 project=X인 객체만 S3에서 검색한다” — 성립하나?",
    a: "성립하지 않는다 — 태그·메타데이터로는 객체를 직접 검색/필터링할 수 없다. 시험 정답 패턴은 DynamoDB 같은 외부 DB에 인덱스 구축이다(쿼리 가능한 S3 Metadata 테이블이 신설됐지만 시험 기준 정답은 여전히 외부 인덱스 — §10 부기).",
  },

  // ── 11 S3 암호화 ──────────────────────────────────────────────────────
  {
    section: "11",
    q: "“키 사용을 우리가 제어하고 사용 이력을 감사해야 한다” — 정답 암호화 방식과 근거는?",
    a: "SSE-KMS + 고객 관리형 KMS 키 — AWS 관리형 aws/s3 키는 키 정책·수명 주기를 직접 제어할 수 없다. 키 사용은 CloudTrail에 감사 기록으로 남고, 헤더 값은 aws:kms.",
  },
  {
    section: "11",
    q: "SSE-KMS 버킷에 대량 업로드하자 ThrottlingException이 난다 — 원인과 해법은?",
    a: "객체마다 KMS API가 호출돼 요청 쿼터를 소모하기 때문 — 업로드는 GenerateDataKey, 다운로드는 Decrypt. 해법은 S3 Bucket Key(KMS 호출 최대 99% 감소) 또는 Service Quotas 한도 상향.",
  },
  {
    section: "11",
    q: "HTTP(비TLS) 요청을 버킷에서 원천 차단하려면 — 정답 패턴은?",
    a: "버킷 정책에서 aws:SecureTransport = false인 요청을 명시적 Deny — 전송 중 암호화 강제의 공식 정책 패턴이다.",
  },

  // ── 12 S3 기본 암호화 ─────────────────────────────────────────────────
  {
    section: "12",
    q: "업로드를 SSE-KMS로만 받도록 강제하려면? 헤더 없이 업로드하면 어떻게 되나?",
    a: "버킷 정책에서 x-amz-server-side-encryption 헤더가 없거나 다른 값인 PUT을 명시적 Deny한다 — 이 강제 정책하에서는 헤더 없는 업로드도 거부된다. 강제 정책이 없을 때만 헤더 없는 업로드에 버킷 기본값이 적용된다(2023-01부터 새 객체는 기본 SSE-S3 자동 암호화).",
  },

  // ── 13 S3 CORS ────────────────────────────────────────────────────────
  {
    section: "13",
    q: "웹사이트 버킷 A의 페이지가 버킷 B의 이미지를 fetch한다 — CORS 구성은 어느 버킷에?",
    a: "요청을 받는 쪽인 B에 설정한다. 오리진 = scheme + host + port — 하나라도 다르면 교차 오리진이고, B의 CORS 구성이 A 오리진을 허용해야 한다. preflight(OPTIONS)는 비단순 요청일 때만 선행되고 단순 GET은 바로 전송된다.",
  },

  // ── 14 S3 MFA Delete ──────────────────────────────────────────────────
  {
    section: "14",
    q: "관리자 IAM 사용자가 콘솔에서 MFA Delete를 활성화하려 한다 — 되나?",
    a: "안 된다 — MFA Delete는 루트 계정만, 콘솔이 아닌 CLI/SDK/API로만 설정할 수 있고, 버전 관리 활성화 버킷이 전제다(조건 3종 세트).",
  },
  {
    section: "14",
    q: "MFA Delete가 켜진 버킷 — MFA 코드가 필요한 작업과 필요 없는 작업을 가르면?",
    a: "필요: 객체 버전 영구 삭제와 버전 관리 상태 변경 — Suspend뿐 아니라 중단 후 재활성화도 포함이다. 불필요: 일반 삭제(삭제 마커 추가)·버전 목록 조회.",
  },

  // ── 15 S3 액세스 로그 ─────────────────────────────────────────────────
  {
    section: "15",
    q: "액세스 로그를 모니터링 대상 버킷 자신에게 기록하도록 설정했다 — 무슨 일이 벌어지나?",
    a: "로그가 로그를 낳는 무한 루프로 스토리지 요금이 폭증한다 — 반드시 별도 버킷에, 같은 리전·같은 계정 조건으로 설정한다.",
  },

  // ── 16 사전 서명된 URL ────────────────────────────────────────────────
  {
    section: "16",
    q: "버킷은 비공개로 유지하면서 로그인 사용자에게만 동영상 다운로드를 허용해야 한다 — 정답과 권한 원리는?",
    a: "Presigned URL — URL을 받은 사람은 URL을 생성한 주체의 권한을 상속한다. IAM 사용자를 새로 만들 필요가 없다는 것이 핵심 이점.",
  },
  {
    section: "16",
    q: "Presigned URL의 만료 시간 — CLI 기본값과 지정 가능한 최대치는?",
    a: "CLI(aws s3 presign)는 기본 3600초(1시간), --expires-in 최대 604,800초 = 7일. 콘솔 생성은 최대 12시간이고, SDK 기본값은 언어·헬퍼마다 다르다.",
  },

  // ── 17 S3 액세스 포인트 ───────────────────────────────────────────────
  {
    section: "17",
    q: "팀·용도별 규칙이 늘어 버킷 정책 하나가 비대해졌다 — 정답 해법은?",
    a: "S3 액세스 포인트 — 용도별 액세스 포인트에 자체 DNS 이름과 정책을 둬 접근 규칙을 분리·단순화한다. 버킷 정책은 액세스 포인트에 권한을 위임하는 축약 정책으로 두어야 AP 정책이 실효한다.",
  },
  {
    section: "17",
    q: "VPC Origin 액세스 포인트로 접근하려면 무엇이 더 필요한가?",
    a: "VPC 엔드포인트(Gateway 또는 Interface) 생성이 필수이고, 엔드포인트 정책에서 대상 버킷·액세스 포인트 접근을 허용해야 한다.",
  },

  // ── 18 S3 Object Lambda ───────────────────────────────────────────────
  {
    section: "18",
    q: "원본 버킷 하나만 두고 분석 환경에는 PII를 마스킹해 제공해야 한다 — 정답 서비스와 구성 순서는?",
    a: "S3 Object Lambda — 반환 직전에 Lambda로 변환하므로 변환본용 버킷 복제가 필요 없다. 구성: 버킷 → 지원 액세스 포인트 → Object Lambda 액세스 포인트 → Lambda → 애플리케이션. (단, 2025-11부터 유지보수 모드 — 기존 고객 전용이고 신규 계정은 일반 Lambda 변환 파이프라인으로 대체. 시험 선지로는 여전히 등장한다.)",
  },
];
