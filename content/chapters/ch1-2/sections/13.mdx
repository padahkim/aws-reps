import { ExamLi, ExamPoint, Fig, Note, Table } from "../../ui";
import { ExecEnvLifecycleSvg, ReservedConcurrencySlider } from "../figs";
import { SelfQuiz } from "../../interactive";

계정(리전)당 동시 실행 기본 한도 <b>1,000</b>(상향 요청 가능). 함수별 <b>예약 동시성(Reserved Concurrency)</b>은 그 함수의 동시 실행 상한을 지정해 격리합니다 — 무료. <b>0으로 설정하면 모든 호출이 차단</b>되어 폭주·버그 함수의 긴급 차단 스위치로도 씁니다. 한 함수가 폭주해 계정 풀 1,000을 다 쓰면 <b>같은 계정의 다른 모든 함수가 스로틀</b>됩니다(예약 동시성이 필요한 이유이자 시험 시나리오 단골).

<ReservedConcurrencySlider />

<Table
  head={["상황", "동작"]}
  rows={[
    ["동기 호출 스로틀", <>즉시 <b>429 TooManyRequestsException</b> 반환 — 재시도는 호출자 책임</>],
    [
      "비동기 호출 스로틀·시스템 오류",
      <>
        이벤트를 큐로 되돌려 <b>최대 6시간</b> 재시도 — 지수 백오프 1초→최대 5분
      </>,
    ],
    [
      "ESM(폴링) 스로틀",
      <>
        <b>폴링 속도를 조절</b>하며 재시도 — 레코드는 소스(스트림·큐)에 남아 있다
      </>,
    ],
  ]}
/>

<Fig caption="INIT → INVOKE → SHUTDOWN. 콜드 스타트는 INIT까지 새로 하고, 웜 호출은 INVOKE만 반복한다.">
  <ExecEnvLifecycleSvg />
</Fig>

<b>콜드 스타트</b>: 새 실행 환경 기동 시 코드 로드 + 핸들러 밖 초기화(INIT)가 실행되어 첫 요청이 지연됩니다 — 지연 폭은 런타임·초기화 코드·패키지 크기에 따라 가변(통상 수백 ms~수 초). <b>프로비저닝된 동시성(Provisioned Concurrency)</b>은 호출 전에 실행 환경을 미리 초기화해 콜드 스타트를 제거하며(유료), <b>Application Auto Scaling</b>의 스케줄/사용률 기반 관리와 결합합니다. 게시된 <b>버전/별칭에만</b> 설정할 수 있습니다($LATEST 불가).

<Note>보완: <b>SnapStart</b>는 초기화 완료 스냅샷에서 실행 환경을 복원해 콜드 스타트를 줄인다 — Java 11+ 외에 Python 3.12+, .NET 8+도 현행 지원.</Note>

<ExamPoint>
  <ExamLi>“한 함수 폭주가 다른 함수를 스로틀” → Reserved Concurrency로 격리.</ExamLi>
  <ExamLi>“문제 함수의 호출을 즉시 중단” → Reserved Concurrency = 0.</ExamLi>
  <ExamLi>“예측 가능한 피크의 콜드 스타트 제거” → Provisioned Concurrency + AAS 스케줄.</ExamLi>
  <ExamLi>Reserved = 한도 보장(무료), Provisioned = 미리 데워둠(유료) — 역할 구분.</ExamLi>
</ExamPoint>

<SelfQuiz items={[
  {
    q: "매일 오전 9시 트래픽 급증 시 콜드 스타트 지연을 없애려면?",
    a: "Provisioned Concurrency + Application Auto Scaling(스케줄 기반). Reserved는 한도 보장일 뿐 콜드 스타트를 없애지 못한다.",
  },
  {
    q: "호출자가 429 TooManyRequestsException을 받았다. 원인은?",
    a: "스로틀링 — 동시성 한도(계정 1,000 또는 함수 Reserved 한도) 초과. 동기 호출이라 에러가 호출자에게 직접 전달된 것.",
  },
]} />
