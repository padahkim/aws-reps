import type { SessionData } from "../../schema";

/**
 * ch1-2 인출 세션 (이슈 #59) — 섹션 01~20 개념 카드 20장 + 도식 1개 + 혼합 7장.
 *
 * why 는 신형식(#89 결정 ③) — 질문 + 게이팅된 모범답. 모든 물음은 같은 섹션 본문이
 * 가르친 재료만 요구한다 (CLAUDE.md Audience 규칙 — #164 가 잡는 결함 유형을 처음부터 회피).
 * diagram: 비동기 호출 흐름(§03) — 이 챕터에서 유일하게 선형 체인이 성립하는 최빈출 흐름.
 *   ESM 은 소스 3종이 병렬이라 체인이 아니고, 동기는 요청-응답 2노드뿐이라 재현 과제가 안 된다.
 * mixed: 앞 챕터(ch0-1·0-2·1-1)의 mixed 가 아직 비어 있어(작성은 #29 흐름) 누적 풀은 이
 *   챕터 몫만으로 시작한다 — 챕터 안에서 시험이 실제로 혼동을 노리는 이웃 쌍(DLQ↔Destinations,
 *   Reserved↔Provisioned, CF Functions↔Lambda@Edge 등)으로 구성.
 */
export const session: SessionData = {
  concepts: [
    // ── 01 서버리스와 Lambda 개요 ───────────────────────────────────────
    {
      id: "c1",
      section: "01",
      q: "“서버리스”의 실제 뜻은 무엇이며, EC2와 Lambda는 실행 시간·과금·확장에서 어떻게 다른가요?",
      a: "서버리스는 서버가 없다는 게 아니라 서버를 관리·프로비저닝하지 않는다는 뜻이다. EC2는 계속 실행되는 가상 서버라 실행 중 항상 과금되고 확장에 ASG 구성 같은 개입이 필요하다. Lambda는 가상 함수로 실행이 최대 15분으로 짧고, 요청 수 + 컴퓨팅 시간(GB-초)으로 과금되며, 스케일링이 완전 자동이다.",
      why: {
        q: "“임의의 Docker 이미지를 실행하고 싶다”는 요구에 Lambda 컨테이너 이미지가 답이 될 수 없는 이유는 무엇일까요?",
        a: "Lambda 컨테이너 이미지는 반드시 Lambda Runtime API를 구현해야 하기 때문이다 — 아무 이미지나 올릴 수 없다. 임의의 Docker 이미지를 그대로 실행하는 요구의 정답은 ECS/Fargate다 (빈출 함정).",
      },
    },
    // ── 02 호출 ① 동기식 ───────────────────────────────────────────────
    {
      id: "c2",
      section: "02",
      q: "동기식 호출에서 오류 처리는 누구 책임이며, CLI에서 호출 유형은 어떤 파라미터의 어떤 값으로 정하나요?",
      a: "오류 처리는 클라이언트 측 책임 — 재시도·지수 백오프를 호출자가 수행한다. 호출 유형은 --invocation-type 파라미터로 정한다: RequestResponse(기본 = 동기), Event(비동기), DryRun(권한·파라미터 검증만).",
      why: {
        q: "ALB 뒤에 Lambda를 두면 요청과 응답은 어떤 변환을 거칠까요? Multi-Value Headers를 켜면 무엇이 달라지나요?",
        a: "함수를 대상 그룹에 등록하면 ALB가 HTTP 요청을 JSON 문서로 변환해 동기 호출하고, 함수의 JSON 응답(statusCode·headers·body)을 다시 HTTP로 변환한다. Multi-Value Headers를 켜면 같은 이름의 쿼리 스트링·헤더가 배열로 변환된다.",
      },
    },
    // ── 03 호출 ② 비동기식 & DLQ ───────────────────────────────────────
    {
      id: "c3",
      section: "03",
      q: "비동기 호출의 재시도 정책을 수치까지 말하고, 그 정책이 처리 로직에 요구하는 성질을 설명해 보세요.",
      a: "이벤트는 내부 이벤트 큐에 쌓이고 호출자는 즉시 202 Accepted만 받는다. 오류 시 총 3회 시도 — 최초 시도 후 1분 대기 → 재시도, 다시 2분 대기 → 재시도. 재시도 때문에 같은 이벤트가 두 번 처리될 수 있으므로 로직은 멱등해야 하고, 최종 실패 이벤트는 DLQ(SNS 토픽 또는 SQS 큐)로 보낸다.",
      why: {
        q: "최종 실패 이벤트가 DLQ에 안 들어온다면 어떤 권한을 의심해야 할까요?",
        a: "실행 역할의 SQS/SNS 쓰기 권한이다 — DLQ 전송은 함수의 실행 역할 권한으로 이루어지므로, 역할에 대상 큐·토픽에 쓸 권한이 없으면 실패 이벤트가 조용히 사라진다 (권한 함정 출제).",
      },
    },
    // ── 04 호출 ③ 이벤트 소스 매핑 ─────────────────────────────────────
    {
      id: "c4",
      section: "04",
      q: "이벤트 소스 매핑의 핵심 대상 3가지를 들고, 호출이 푸시·폴링 중 무엇이고 동기·비동기 중 무엇인지 말해 보세요.",
      a: "Kinesis Data Streams, DynamoDB Streams, SQS(+FIFO). Lambda가 직접 폴링해서 레코드로 배치를 구성하고, 함수를 동기식으로 호출한다 — S3·SNS·EventBridge의 비동기(푸시)와 대비되는 구분이 최다 빈출이다.",
      why: {
        q: "스트림에서 불량 레코드 하나가 샤드 전체를 막는 이유는 무엇이고, 어떤 설정으로 풀까요?",
        a: "스트림 오류의 기본 동작이 “성공하거나 항목이 만료될 때까지 배치 전체 재시도”라서다 — 샤드 수준 순서 보장 때문에 그동안 해당 샤드 처리가 중지된다. 대응은 bisect batch on error(배치 반씩 쪼개 재시도)·재시도 제한·discard old events, 폐기 이벤트는 Destination으로.",
      },
    },
    // ── 05 이벤트 & 컨텍스트 객체 ──────────────────────────────────────
    {
      id: "c5",
      section: "05",
      q: "핸들러가 받는 이벤트 객체와 컨텍스트 객체는 무엇이 다른가요? 각각에 들어 있는 것을 예로 들어 보세요.",
      a: "이벤트 객체는 호출 서비스가 보낸 데이터다 — Records, source, 요청 본문. 컨텍스트 객체는 호출·런타임 메타데이터다 — aws_request_id, function_name, memory_limit_in_mb, 로그 그룹/스트림. “데이터 vs 메타데이터”가 구분 축이다.",
      why: {
        q: "타임아웃 직전에 정리 작업을 하고 싶다면 무엇을 쓸까요?",
        a: "context.get_remaining_time_in_millis() — 컨텍스트 객체에서 남은 실행 시간을 확인해 타임아웃 전에 정리 로직을 태울 수 있다 (시험 포인트).",
      },
    },
    // ── 06 Lambda Destinations ─────────────────────────────────────────
    {
      id: "c6",
      section: "06",
      q: "DLQ와 Destinations를 적용 범위와 대상 서비스 축으로 대조해 보세요.",
      a: "DLQ는 비동기 호출의 “실패”만, 대상은 SQS·SNS 2종. Destinations는 비동기 호출의 성공+실패 모두에 각각 지정할 수 있고 스트림 ESM(Kinesis·DDB Streams)의 폐기 배치도 커버하며, 대상은 SQS·SNS·Lambda·EventBridge 4종이다.",
      why: {
        q: "공식 문서가 Destinations를 DLQ의 “대안”으로 두는 근거 두 가지는 무엇일까요?",
        a: "① 실패만이 아니라 성공/실패 모두 지원하고 ② 이벤트 본문 위주인 DLQ와 달리 호출 컨텍스트·응답 등 더 풍부한 정보를 전송하기 때문이다 — “실패 이벤트를 더 많은 컨텍스트와 함께”가 보이면 Destinations가 정답 방향이다.",
      },
    },
    // ── 07 권한 — 실행 역할 vs 리소스 기반 정책 ─────────────────────────
    {
      id: "c7",
      section: "07",
      q: "Lambda 권한의 방향 규칙 — 어떤 경우가 실행 역할이고 어떤 경우가 리소스 기반 정책인지 말해 보세요.",
      a: "나가는 권한 = 실행 역할: 함수가 다른 서비스에 접근할 때, 그리고 ESM처럼 Lambda가 소스를 폴링해 읽어올 때. 들어오는 호출 권한 = 리소스 기반 정책: S3 이벤트 알림처럼 서비스가 함수를 직접 호출(푸시)할 때. 동일 계정에서는 둘 중 하나만 허용해도 호출된다.",
      why: {
        q: "ESM은 “다른 서비스가 Lambda로 데이터를 보내는” 그림처럼 보이는데, 왜 소스를 읽는 권한이 실행 역할 쪽에 있어야 할까요?",
        a: "폴링 주체가 Lambda 자신이기 때문이다 — 스트림·큐가 함수를 부르는 게 아니라 Lambda가 소스로 “나가서” 읽어온다. 방향이 나가는 쪽이므로 권한도 실행 역할에 있어야 한다.",
      },
    },
    // ── 08 환경 변수 ───────────────────────────────────────────────────
    {
      id: "c8",
      section: "08",
      q: "환경 변수의 총 용량 한도와, 비밀 값을 안전하게 두는 선택지들을 말해 보세요.",
      a: "총 4KB. 비밀 값은 KMS로 암호화하거나(Lambda 서비스 키 또는 고객 관리형 키), Secrets Manager / SSM Parameter Store를 참조한다. 용도 자체는 코드를 재배포하지 않고 함수 동작을 조정하는 것 — “환경별 설정을 코드 수정 없이”의 정답이다.",
    },
    // ── 09 모니터링 & X-Ray ────────────────────────────────────────────
    {
      id: "c9",
      section: "09",
      q: "Lambda에서 X-Ray 추적을 켜는 절차와 실행 역할에 필요한 권한은 무엇인가요?",
      a: "구성에서 Active Tracing을 켜면 Lambda가 X-Ray 데몬을 대신 실행해 주고, 코드에서는 X-Ray SDK만 쓰면 된다. 실행 역할에는 AWSXRayDaemonWriteAccess가 필요하다. 로그는 CloudWatch Logs에 자동 저장되지만 이것도 로그 쓰기 권한(AWSLambdaBasicExecutionRole)이 전제다.",
      why: {
        q: "스트림 처리가 밀리고 있는지는 어떤 지표로 확인할까요?",
        a: "IteratorAge — 스트림 레코드가 생성된 뒤 처리까지 얼마나 묵었는지를 보여 주는 지표라, 값이 커지면 ESM 처리가 스트림 유입 속도를 못 따라가고 있다는 신호다.",
      },
    },
    // ── 10 Lambda@Edge & CloudFront Functions ──────────────────────────
    {
      id: "c10",
      section: "10",
      q: "CloudFront Functions와 Lambda@Edge의 선택 기준을 트리거 지점·실행 시간·기능 접근 축으로 말해 보세요.",
      a: "CloudFront Functions는 Viewer Request/Response 2지점 전용, 1ms 미만, JavaScript 전용 — 캐시 키 정규화·헤더 조작·URL 재작성 같은 초경량 작업용이다. Lambda@Edge는 4개 후크 지점 전부, 최대 30초, 네트워크·파일시스템·바디 접근이 가능해 무거운 로직·외부 서비스 호출용이다.",
      why: {
        q: "Lambda@Edge를 왜 하필 us-east-1에서 작성해야 할까요?",
        a: "us-east-1에서 작성한 함수를 CloudFront가 전 세계 엣지 로케이션으로 복제해 주는 구조이기 때문이다 — 각 엣지에 직접 배포하는 게 아니라 한 리전에서 쓰고 복제를 맡긴다.",
      },
    },
    // ── 11 VPC의 Lambda ────────────────────────────────────────────────
    {
      id: "c11",
      section: "11",
      q: "기본 상태의 Lambda가 접근할 수 있는 것과 없는 것, 그리고 VPC 리소스에 접근하게 만드는 방법을 말해 보세요.",
      a: "기본적으로 Lambda는 AWS 소유 VPC(내 VPC 밖)에서 실행된다 — 퍼블릭 인터넷·퍼블릭 API·DynamoDB는 되지만 내 VPC 안의 RDS·ElastiCache·내부 ELB는 안 된다. VPC ID·서브넷·보안 그룹을 지정하면 서브넷에 ENI를 만들어 접근하며, 실행 역할에 AWSLambdaVPCAccessExecutionRole이 필요하다.",
      why: {
        q: "VPC에 넣은 Lambda를 퍼블릭 서브넷에 배치하면 인터넷이 될까요? 이유와 정답 구성은?",
        a: "안 된다 — Lambda는 퍼블릭 서브넷에서도 공인 IP를 갖지 못한다는 점이 EC2와 다르고, 가장 유명한 시험 함정이다. 인터넷이 필요하면 프라이빗 서브넷 + NAT Gateway/Instance, AWS 서비스라면 NAT 없이 VPC 엔드포인트로 간다.",
      },
    },
    // ── 12 함수 성능 ───────────────────────────────────────────────────
    {
      id: "c12",
      section: "12",
      q: "“CPU 바운드 작업이 느리다”의 정답 패턴과 그 근거 수치를 말해 보세요.",
      a: "RAM을 늘린다 — CPU는 직접 설정할 수 없고 RAM(128MB~10,240MB, 1MB 단위)에 비례해 할당되며, 약 1,769MB에서 1 vCPU 상당에 도달한다. 그 이상을 쓰려면 멀티스레딩 코드를 작성해야 활용된다.",
      why: {
        q: "DB 연결이나 SDK 클라이언트 초기화를 핸들러 밖에 두는 이유는 무엇일까요?",
        a: "실행 컨텍스트가 일정 시간 유지되어 다음 호출에서 재사용되기 때문이다 — 핸들러 밖 코드는 콜드 스타트의 INIT에서 한 번만 실행되고 웜 호출은 그 연결을 그대로 재사용한다. 핸들러 안에 두면 매 호출마다 연결을 새로 만든다 (코드 문제 단골).",
      },
    },
    // ── 13 동시성 & 콜드 스타트 ────────────────────────────────────────
    {
      id: "c13",
      section: "13",
      q: "Reserved Concurrency와 Provisioned Concurrency의 역할·비용 차이를 말해 보세요.",
      a: "Reserved는 함수별 동시 실행 상한을 지정해 격리하는 기능 — 무료이고, 0으로 설정하면 모든 호출이 차단되어 긴급 차단 스위치로도 쓴다. Provisioned는 호출 전에 실행 환경을 미리 초기화해 콜드 스타트를 제거하는 기능 — 유료이고, 게시된 버전/별칭에만 설정할 수 있다($LATEST 불가).",
      why: {
        q: "한 함수의 폭주가 왜 같은 계정의 “다른” 함수들까지 스로틀시킬까요?",
        a: "계정(리전)당 동시 실행 한도 기본 1,000을 모든 함수가 공유하기 때문이다 — 한 함수가 풀을 다 쓰면 나머지 전부가 스로틀된다. 그래서 중요한 함수는 Reserved Concurrency로 몫을 떼어 격리한다 (시나리오 단골).",
      },
    },
    // ── 14 레이어 & 스토리지 ───────────────────────────────────────────
    {
      id: "c14",
      section: "14",
      q: "레이어의 두 가지 용도와 한도를 말해 보세요.",
      a: "① 커스텀 런타임(C++·Rust) ② 종속성 재사용 — 무거운 라이브러리를 분리해 함수 패키지를 가볍게 하고 여러 함수가 공유한다. 한도는 함수당 최대 5개, 함수+레이어 압축 해제 합산 250MB.",
      why: {
        q: "스토리지 4종(/tmp·레이어·S3·EFS) 중 함수 간 공유가 안 되는 것은? 그리고 EFS 마운트의 전제는?",
        a: "/tmp만 공유 불가(실행 컨텍스트의 임시 디스크). EFS는 함수가 VPC 안에서 실행될 때 같은 VPC의 EFS를 마운트하며 EFS Access Point가 필수다 — 함수 인스턴스당 연결 1개라 동시성 폭증 시 연결·버스트 한도에 주의한다.",
      },
    },
    // ── 15 배포 ────────────────────────────────────────────────────────
    {
      id: "c15",
      section: "15",
      q: "zip이 50MB를 넘으면 어떻게 배포하나요? CloudFormation 인라인 배포의 제약은?",
      a: "50MB 미만은 직접 업로드, 초과하면 S3에 올린 뒤 참조한다. CloudFormation 인라인(Code.ZipFile)은 간단한 함수용으로 외부 종속성을 포함할 수 없다. AWS SDK는 기본 포함이라 별도 패키징이 불필요하다 (시험 포인트).",
      why: {
        q: "S3의 코드만 새로 올렸는데 CloudFormation이 함수를 업데이트하지 않았습니다 — 왜, 그리고 정답은?",
        a: "템플릿의 S3Bucket·S3Key·S3ObjectVersion이 그대로면 CFN은 변경으로 보지 않기 때문이다. 정답은 버킷 버저닝을 켜고 S3ObjectVersion을 매번 갱신하는 것 (CFN 함정).",
      },
    },
    // ── 16 버전 & Alias ────────────────────────────────────────────────
    {
      id: "c16",
      section: "16",
      q: "$LATEST·게시 버전·별칭의 가변/불변을 구분하고, 가중치 라우팅의 규칙을 말해 보세요.",
      a: "$LATEST는 가변 작업본. 게시(publish)하면 V1, V2 … 불변 버전이 되고 코드+구성(환경 변수 포함)이 고유 ARN으로 고정된다. Alias는 버전을 가리키는 가변 포인터다. 가중치 라우팅은 최대 2개 버전 사이에서만(예: V1 95% / V2 5%), 별칭이 별칭을 가리키는 건 불가, 가중치·Provisioned Concurrency는 게시된 버전/별칭에만 된다.",
      why: {
        q: "블루/그린 배포에서 사용자에게 고정으로 남는 것과 뒤에서 교체되는 것은 각각 무엇일까요?",
        a: "사용자에게는 별칭의 안정적인 ARN이 고정으로 남고, 별칭이 가리키는 게시 버전만 뒤에서 교체된다 — 별칭이 “가변 포인터”라는 성질이 블루/그린을 성립시킨다.",
      },
    },
    // ── 17 CodeDeploy 트래픽 전환 ──────────────────────────────────────
    {
      id: "c17",
      section: "17",
      q: "CodeDeploy의 Linear·Canary·AllAtOnce 세 전략의 동작 차이를 말해 보세요.",
      a: "Linear는 N분마다 일정 비율씩 증가(Linear10PercentEvery3Minutes), Canary는 X%로 시험 후 한 번에 100%(Canary10Percent5Minutes), AllAtOnce는 즉시 100% 전환 — 가장 빠르고 위험하다. CodeDeploy가 자동화하는 대상은 Lambda Alias의 트래픽 전환이다.",
      why: {
        q: "배포 전후 검증과 자동 롤백은 각각 무엇으로 구현하나요?",
        a: "Pre/Post Traffic Hook(Lambda 함수)으로 배포 전후를 검증하고, CloudWatch Alarm이 울리면 자동 롤백한다. AppSpec 필수 4필드는 Name·Alias·CurrentVersion·TargetVersion.",
      },
    },
    // ── 18 함수 URL ────────────────────────────────────────────────────
    {
      id: "c18",
      section: "18",
      q: "함수 URL은 무엇에 설정할 수 있고, 접근 제어와 트래픽 제한은 각각 무엇으로 하나요?",
      a: "alias 또는 $LATEST에만 설정할 수 있다(특정 버전 불가). 접근 제어는 리소스 기반 정책(계정·IP CIDR)과 AuthType(NONE — 그래도 리소스 정책의 허용 명시 필요 / AWS_IAM), 트래픽 제한은 Reserved Concurrency로 한다. 퍼블릭 인터넷 접근용 기능이다.",
    },
    // ── 19 익스텐션 · 테스트 · Firehose ────────────────────────────────
    {
      id: "c19",
      section: "19",
      q: "SAM CLI의 sam local invoke와 sam local start-api는 각각 무엇을 하나요? 컨테이너 이미지의 로컬 테스트는?",
      a: "sam local invoke는 이벤트 파일로 함수를 1회 호출하고, sam local start-api는 로컬에서 API Gateway를 에뮬레이션한다. 컨테이너 이미지는 Lambda Runtime Interface Emulator(RIE)로 로컬 실행한다. 단위 테스트는 비즈니스 로직을 핸들러에서 분리해 순수 함수로 두는 게 정석.",
      why: {
        q: "Firehose 준실시간 변환에서 Lambda 함수가 레코드마다 반환해야 하는 세 가지는 무엇일까요?",
        a: "recordId, 처리 결과(Ok / Dropped / ProcessingFailed), 변환된 data — “준실시간(near real-time) 데이터 변환” 키워드가 보이면 Firehose + Lambda 변환이 정답 패턴이다.",
      },
    },
    // ── 20 한도 총정리 ─────────────────────────────────────────────────
    {
      id: "c20",
      section: "20",
      q: "동기·비동기 호출의 페이로드 한도와, 한도를 넘는 큰 데이터의 정답 패턴을 말해 보세요.",
      a: "동기 6MB(요청·응답 각각), 비동기 1MB, 스트리밍 응답 200MB. 큰 데이터는 본문에 싣지 않고 S3에 두고 참조를 전달한다. “비동기 256KB”는 SQS 메시지 한도와 혼동한 구식 수치다.",
      why: {
        q: "“30분 걸리는 배치 작업” 요구에 Lambda가 탈락하는 근거와 정답 방향은 무엇일까요?",
        a: "실행 시간 한도가 최대 900초(15분)라서다. 정답 방향은 ECS/Fargate/Batch로 옮기거나 Step Functions로 작업을 분할하는 것 — 같은 논리로 “3GB 임시 파일”은 /tmp(10GB)로 되고, “300MB 종속성”은 컨테이너 이미지(10GB)나 EFS로 간다.",
      },
    },
  ],

  diagram: {
    prompt:
      "S3 업로드가 비동기 호출로 처리되고 실패가 DLQ에 닿기까지 — 각 상자의 역할만 보고 무엇인지 떠올린 뒤 탭해서 확인하세요.",
    nodes: [
      { role: "객체 업로드가 이벤트를 만든다", name: "S3 이벤트 알림" },
      { role: "이벤트를 쌓고 202만 돌려준다", name: "내부 이벤트 큐" },
      { role: "이벤트를 꺼내 코드를 실행한다", name: "Lambda 함수" },
      { role: "3회 모두 실패한 이벤트가 닿는 곳", name: "DLQ (SQS·SNS)" },
    ],
    edges: ["비동기 호출 · 202 Accepted", "1분 → 2분 간격 재시도", "실행 역할의 쓰기 권한으로 전송"],
  },

  mixed: [
    {
      id: "m1",
      scenario: "비동기 호출의 “실패 이벤트만” 따로 모아 두고 싶다",
      service: "DLQ (SQS·SNS)",
      why: "비동기 호출 전용 실패 수집 장치 — 전송은 실행 역할의 SQS/SNS 쓰기 권한으로 이루어진다.",
      contrast: "성공 결과까지 라우팅하거나 더 풍부한 호출 컨텍스트가 필요하면 Destinations.",
    },
    {
      id: "m2",
      scenario: "성공/실패 결과를 모두 다른 서비스로 라우팅하고, 호출 컨텍스트까지 받고 싶다",
      service: "Lambda Destinations",
      why: "비동기 성공+실패 각각에 SQS·SNS·Lambda·EventBridge 4종을 지정할 수 있고 ESM 폐기 배치도 커버한다.",
      contrast: "DLQ는 비동기 “실패”만, 대상도 SQS·SNS 2종뿐이다.",
    },
    {
      id: "m3",
      scenario: "한 함수의 폭주로부터 같은 계정의 다른 함수들을 지키고 싶다 — 추가 비용 없이",
      service: "Reserved Concurrency",
      why: "함수별 동시 실행 상한을 지정해 계정 공유 풀(기본 1,000)에서 격리한다 — 무료, 0이면 긴급 차단 스위치.",
      contrast: "Provisioned Concurrency는 콜드 스타트 제거용(유료) — 상한 격리가 아니다.",
    },
    {
      id: "m4",
      scenario: "예측 가능한 피크 시간대의 콜드 스타트를 없애고 싶다",
      service: "Provisioned Concurrency",
      why: "호출 전에 실행 환경을 미리 초기화해 두는 유료 기능 — Application Auto Scaling 스케줄과 결합하고, 게시된 버전/별칭에만 설정된다.",
      contrast: "Reserved Concurrency는 상한 지정·격리(무료) — 환경을 데워 두지 않는다.",
    },
    {
      id: "m5",
      scenario: "뷰어 요청 단계에서 1ms 미만짜리 초경량 헤더 조작·URL 재작성을 하고 싶다",
      service: "CloudFront Functions",
      why: "Viewer Request/Response 전용 JavaScript 함수 — 초당 수백만 요청, @Edge 대비 약 1/6 가격.",
      contrast: "오리진 단계 개입·네트워크/파일시스템/바디 접근·수 ms 이상 로직이면 Lambda@Edge(4지점·최대 30초).",
    },
    {
      id: "m6",
      scenario: "이벤트 소스 매핑(SQS)에서 실패한 메시지를 모으고 싶다 — DLQ는 어디에 설정?",
      service: "SQS 큐 자체의 DLQ",
      why: "ESM의 실패 배치는 개별 항목 단위로 큐에 복귀하므로, 실패분은 소스 큐의 redrive 정책이 DLQ로 모은다.",
      contrast: "Lambda의 DLQ 설정은 비동기 호출 전용이라 ESM에서는 동작하지 않는다(핵심 함정). 폐기 배치를 Destination으로 보내는 것도 스트림 소스(Kinesis·DDB Streams) 얘기다 — SQS ESM에는 못 붙인다.",
    },
    {
      id: "m7",
      scenario: "API Gateway·ALB 없이 함수에 퍼블릭 HTTPS 엔드포인트를 달고 싶다",
      service: "Lambda 함수 URL",
      why: "고유·불변 URL(https://{url-id}.lambda-url.{region}.on.aws)을 alias/$LATEST에 부여 — CORS 지원, 접근 제어는 리소스 기반 정책.",
      contrast: "HTTP 요청↔JSON 변환을 로드 밸런서 계층에서 하려면 ALB 대상 그룹 등록 — 함수 URL은 관문 없이 함수가 직접 받는다.",
    },
  ],
};
