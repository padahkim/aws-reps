import type { SessionData } from "../../schema";

/**
 * ch3-1 인출 세션 (#272) — 14개 섹션 전부에 개념 카드 19장.
 *
 * 배분은 기본 1장, 서술로 정리할 덩어리가 둘인 섹션만 2장 — 05(용도 구분 / 수명과 상한)·
 * 07(베어러 토큰 전달 / 토큰별 검증 차이)·11(교환 흐름 / 신뢰 정책)·12(격리 기법 / 조건 키)·
 * 13(오프로드의 의미 / 미인증 처리)이 그렇다.
 * why 는 #89 결정 ③의 신형식(질문 + 게이팅된 모범답)으로, 인과·기전이 실재하는 카드에만
 * 달았다 — 사실 나열형 카드에 억지 why 를 붙이지 않는다.
 * 셀프 퀴즈(./selfquiz.ts 35문항)와 주제가 겹치는 것은 허용하되 문장 재탕은 금지(#98) —
 * 카드는 구조·인과를 서술로 묻고, 셀프 퀴즈는 판정형 핵심 사실을 맡는다.
 *
 * 물음은 전부 본문이 가르친 것만 묻는다 (CLAUDE.md Audience).
 */
export const session: SessionData = {
  concepts: [
    // ── 01 Cognito 개요 ─────────────────────────────────────────────────
    {
      id: "c1",
      section: "01",
      q: "Cognito 안에 든 두 서비스를 “묻는 것 · 하는 일 · 내주는 것” 세 축으로 갈라 설명해 보세요.",
      a: "• User Pool은 “너 누구야?”를 묻는다(인증) — 회원가입·로그인을 처리하고 사용자를 저장하며, 내주는 것은 JWT 토큰이다.\n• Identity Pool은 “뭘 할 수 있어?”를 묻는다(인가) — 이미 로그인한 사용자에게 AWS 접근 권한을 주며, 내주는 것은 STS가 발급한 임시 AWS 자격 증명이다.\n두 축을 가르는 가장 빠른 기준은 세 번째다 — 산출물이 JWT면 User Pool, 임시 자격 증명이면 Identity Pool.",
      why: {
        q: "로그인과 AWS 권한 부여가 한 서비스에 묶이지 않고 둘로 나뉘어 있는 이유는 무엇일까요?",
        a: "두 일이 필요해지는 시점이 다르기 때문이다. 앱 대부분은 “내 백엔드에 로그인”까지만 필요하고, 그 사용자가 AWS 리소스를 직접 만질 일은 없다 — 파일 업로드처럼 직접 접근이 필요한 앱만 두 번째가 필요하다. 둘을 한 덩어리로 묶으면 로그인만 필요한 앱까지 IAM 역할 설계를 떠안게 된다.",
      },
    },

    // ── 02 IAM과 Cognito의 구분 ─────────────────────────────────────────
    {
      id: "c2",
      section: "02",
      q: "IAM과 Cognito는 무엇을 기준으로 갈리나요? 기능이 아닌 그 기준을 말해 보세요.",
      a: "사용자가 누구인가로 갈린다. IAM은 내가 신뢰하는 내부 주체(개발자·관리자·계정 안의 서비스)를, Cognito는 AWS 계정 밖의 외부 앱 사용자를 맡는다. 규모도 따라 갈린다 — IAM은 수십~수백 명, Cognito는 수천~수백만 명이다. Cognito는 IAM을 대체하지 않는다 — 외부 사용자를 IAM 역할에 연결해 주는 다리다.",
      why: {
        q: "“앱 고객마다 IAM 사용자를 만든다”가 왜 언제나 오답일까요?",
        a: "IAM 사용자는 계정당 5,000명 한도라 십만 명 규모에서 애초에 담기지 않는다. 게다가 IAM 사용자는 계정 안의 주체라 하나 만들 때마다 내 계정의 관리 대상이 늘어난다 — 외부 고객은 계정 안에 자리를 차지하지 않아야 하고, 그래서 신원을 밖에 두고 필요할 때 역할만 빌려주는 구조가 나왔다.",
      },
    },

    // ── 03 User Pool = 인증 ─────────────────────────────────────────────
    {
      id: "c3",
      section: "03",
      q: "User Pool이 대신해 주는 일을 네 가지 이상 들고, 그중 이름 때문에 헷갈리는 기능 하나를 짚어 보세요.",
      a: "사용자 저장(서버리스 사용자 DB), 간편 로그인과 비밀번호 재설정, 이메일·전화번호 확인, MFA, 유출된 자격 증명 차단, 그리고 연합 로그인이다.\n헷갈리는 것은 마지막 연합 로그인(Federation)이다 — Google·Facebook·SAML·OIDC 로그인은 User Pool 자체 기능이라 Identity Pool이 없어도 된다. Identity Pool의 옛 이름이 “Federated Identities”라서 생기는 혼동이다.",
    },

    // ── 04 CUP 로그인 흐름 ──────────────────────────────────────────────
    {
      id: "c4",
      section: "04",
      q: "로그인 흐름에서 비밀번호와 토큰이 각각 누구와 누구 사이를 오가는지 말해 보세요.",
      a: "비밀번호는 사용자와 User Pool 사이에서만 오간다. 토큰은 User Pool이 사용자에게 주고, 사용자가 다시 백엔드(API Gateway·ALB)에 제시한다. 백엔드는 이 대화에 끼지 않으며 비밀번호를 보지 못한다 — 받은 토큰을 검증할 뿐이다.",
      why: {
        q: "소셜 로그인을 써도 앱이 받는 토큰이 언제나 User Pool 발급인 것이 왜 편한가요?",
        a: "앱 코드가 로그인 경로를 몰라도 되기 때문이다. 구글로 들어왔든 직접 가입했든 앱이 다루는 토큰의 형식과 발급자가 같아서, 새 소셜 제공자를 추가해도 앱의 검증 로직은 그대로다 — 변하는 것은 User Pool 설정뿐이다.",
      },
    },

    // ── 05 토큰 3종 ─────────────────────────────────────────────────────
    {
      id: "c5",
      section: "05",
      q: "로그인 한 번에 나오는 토큰 세 개의 용도를 각각 구분해 설명해 보세요.",
      a: "• ID 토큰 — 사용자가 누구인지를 담는다(이름·이메일·sub 같은 신원 클레임). 백엔드에 “이 사람이다”를 알린다.\n• Access 토큰 — 사용자가 무엇을 해도 되는지를 담는다(OAuth 2.0 스코프). API 호출을 인가하는 데 쓴다.\n• Refresh 토큰 — 앞의 두 토큰을 다시 발급받는 데만 쓴다. 사용자를 재로그인시키지 않기 위한 것이다.",
    },
    {
      id: "c6",
      section: "05",
      q: "세 토큰의 기본 수명이 왜 서로 다른지, 그리고 그 사이에 걸린 제약 하나를 설명해 보세요.",
      a: "ID·Access는 기본 60분, Refresh는 기본 30일이다. 차이는 탈취됐을 때의 노출 빈도에서 온다 — ID·Access는 요청마다 네트워크에 실려 나가 새어 나갈 기회가 많으니 짧게, Refresh는 앱이 보관한 채 재발급 때만 쓰니 길게 둔다.\n제약: ID·Access의 유효기간은 Refresh의 유효기간을 넘을 수 없다. 재발급 수단이 먼저 만료되는 조합은 성립하지 않기 때문이다.",
      why: {
        q: "“사용자가 자꾸 다시 로그인해야 한다”는 불만에 Access 토큰을 늘리는 처방이 왜 틀렸을까요?",
        a: "재로그인 시점을 정하는 것은 Refresh 토큰이기 때문이다. Access가 만료돼도 Refresh가 살아 있으면 앱이 조용히 재발급받아 사용자는 아무것도 느끼지 못한다 — 로그인 화면이 뜨는 순간은 Refresh까지 만료된 때다. Access만 늘리면 재로그인 주기는 그대로인 채 탈취된 토큰의 유효 시간만 길어진다.",
      },
    },

    // ── 06 JWT 구조 ─────────────────────────────────────────────────────
    {
      id: "c7",
      section: "06",
      q: "JWT의 세 조각을 들고, Payload를 다룰 때 지켜야 할 원칙을 그 성질에서 끌어내 보세요.",
      a: "Header(서명 알고리즘·키 ID) · Payload(사용자 정보 = 클레임) · Signature(위·변조 검증용 서명)를 점으로 이어 붙인 한 줄 문자열이다.\nPayload는 암호화가 아니라 서명이라 base64 디코딩만 하면 그대로 보인다 — 그래서 비밀을 담지 않는다. 서명이 막는 것은 내용이 새는 것이 아니라 내용이 바뀌는 것이다.",
      why: {
        q: "이메일이나 사용자 이름 대신 sub를 데이터의 키로 삼는 이유는 무엇일까요?",
        a: "sub는 그 사용자의 불변 고유 UUID이기 때문이다. 이메일·전화번호·preferred_username 같은 속성은 바뀔 수 있어서 그것을 키로 저장해 두면 변경 순간 과거 데이터와의 연결이 끊긴다. 실제 username도 계정 생성 뒤 바꿀 수 없지만, 사용자 식별의 정본은 sub다. 이 성질이 §12의 정책 변수로 이어진다 — 정책이 참조하는 값도 결국 이 sub다.",
      },
    },

    // ── 07 API Gateway 통합 ─────────────────────────────────────────────
    {
      id: "c8",
      section: "07",
      q: "앱이 토큰을 API Gateway에 어떤 형태로 보내는지, 그 이름과 위치를 말해 보세요.",
      a: "Authorization 헤더에 싣는다. REST API의 Cognito 사용자 풀 권한 부여자는 “Authorization: <JWT 원문>”으로 받고, HTTP API의 JWT 권한 부여자는 JWT 원문 또는 “Authorization: Bearer <JWT>”를 받는다. 쿼리 문자열이나 바디가 아니다. 토큰은 가진 사람에게 권한을 주므로 그 자체가 곧 열쇠다.",
    },
    {
      id: "c9",
      section: "07",
      q: "Access 토큰을 보낼 때와 ID 토큰을 보낼 때 API Gateway가 하는 일이 어떻게 다른가요?",
      a: "Access 토큰이면 메서드에 설정한 인가 스코프(Authorization scopes)와 토큰의 OAuth 2.0 스코프가 맞는지 검사한다 — 메서드·경로 단위로 권한을 가를 수 있다.\nID 토큰이면 유효하고 만료되지 않았는지 확인한 뒤 통과시키고, 토큰 내용을 백엔드로 넘겨 준다 — 백엔드가 사용자 정보를 그대로 받아 쓴다.\n스코프로 가르려면 Access 토큰이어야 한다. ID 토큰에는 스코프 개념이 없다.",
    },

    // ── 08 Hosted UI ────────────────────────────────────────────────────
    {
      id: "c10",
      section: "08",
      q: "Hosted UI가 무엇을 대신해 주는지와, 커스텀 도메인을 붙일 때의 제약 하나를 말해 보세요.",
      a: "로그인·가입·비밀번호 재설정 페이지를 Cognito가 대신 호스팅한다 — 앱은 그 주소로 리다이렉트만 하고, 로고·CSS로 외관만 손보면 된다.\n제약: 커스텀 도메인을 쓰려면 ACM 인증서가 반드시 us-east-1(버지니아 북부)에 있어야 한다. 내 앱이 어느 리전에 있든 마찬가지이고, 다른 리전의 인증서는 설정 화면 목록에 아예 뜨지 않는다. CloudFront와 같은 규칙이라 세트로 외운다.",
    },

    // ── 09 Lambda 트리거 ────────────────────────────────────────────────
    {
      id: "c11",
      section: "09",
      q: "Lambda 트리거가 걸리는 네 시점을 순서대로 들고, 이름만으로 실행 시점을 읽는 법을 말해 보세요.",
      a: "가입(Sign-up) → 확인(Confirm) → 로그인(Auth) → 토큰 발급(Token) 순이고, 각 시점의 앞뒤로 트리거가 붙는다. 이름의 Pre는 그 일이 일어나기 전, Post는 끝난 뒤다 — Pre Sign-up은 가입을 받아들이기 전, Post Confirmation은 가입 확인이 끝난 뒤다.\n시점에 매이지 않는 것도 있다 — User Migration(기존 DB 이관), Custom Message(메시지 문구), Custom Auth Flow(커스텀 챌린지).",
      why: {
        q: "User Migration 트리거는 기존 비밀번호 해시를 옮기지 않고 어떻게 사용자를 이전할까요?",
        a: "한꺼번에 옮기지 않고 그 사용자가 처음 로그인하는 순간에 옮기기 때문이다. 앱 클라이언트에서 USER_PASSWORD_AUTH 또는 ADMIN_USER_PASSWORD_AUTH를 활성화하고 그 흐름으로 로그인하면 Cognito가 방금 입력한 비밀번호를 마이그레이션 Lambda에 전달한다. Lambda가 옛 DB에 인증해 성공하면 User Pool에 사용자를 만든다 — 저장된 해시를 옮길 필요가 없다. SRP는 비밀번호 원문을 보내지 않으므로 이 단계에 쓸 수 없다.",
      },
    },

    // ── 10 적응형 인증 ──────────────────────────────────────────────────
    {
      id: "c12",
      section: "10",
      q: "적응형 인증이 푸는 문제와, 위험 점수 세 단계의 대응을 설명해 보세요.",
      a: "늘 MFA를 요구하면 사용자가 피곤하고 아예 안 하면 위험하다는 절충을 푼다 — 수상할 때만 더 묻는다. 낮음(Low)은 그대로 통과, 중간(Medium)은 MFA 추가 요구, 높음(High)은 MFA 요구 또는 차단이다.\n판단 근거는 새 디바이스·평소와 다른 위치(IP)·비정상 로그인 패턴이고, 시도와 점수는 CloudWatch Logs로 내보내 사후 조사할 수 있다.",
    },

    // ── 11 Identity Pool = 인가 ─────────────────────────────────────────
    {
      id: "c13",
      section: "11",
      q: "Identity Pool이 토큰을 받고 나서 임시 자격 증명이 사용자 손에 들어가기까지의 단계를 말해 보세요.",
      a: "① 사용자가 IdP(User Pool·Google·SAML 등)에 로그인해 공급자 토큰을 받는다. User Pool을 공급자로 쓸 때는 ID 토큰이다. ② 그 토큰을 Identity Pool에 넘기고, Identity Pool이 토큰이 진짜인지 검증한다. ③ 검증되면 Identity Pool이 STS를 호출해 IAM 역할 기반 임시 자격 증명으로 바꾼다. ④ 사용자는 그 자격 증명으로 S3·DynamoDB를 직접 호출한다.\nIdentity Pool은 사용자를 저장하지 않는다 — 교환만 한다.",
    },
    {
      id: "c14",
      section: "11",
      q: "AWS 계정 밖의 사람이 어떻게 IAM 역할을 맡을 수 있는지, 신뢰 정책으로 설명해 보세요.",
      a: "IAM 역할의 신뢰 정책 Principal에 cognito-identity.amazonaws.com을 적어 두면 “Cognito Identity가 검증해 데려온 사용자라면 이 역할을 맡아도 좋다”가 된다 — ch0-2 §04에서 본 Principal 필드의 쓰임 그대로다. 실제 임시 자격 증명 발급은 ch0-2 §07의 STS가 한다. 역할은 인증된 사용자용과 게스트용을 각각 지정하고, 규칙(Rules)으로 속성에 따라 더 세분할 수 있다.",
      why: {
        q: "Identity Pool이 직접 자격 증명을 만들지 않고 STS를 거치는 구조가 왜 자연스러운가요?",
        a: "임시 자격 증명 발급은 이미 STS의 일이기 때문이다. IAM 사용자가 역할을 맡을 때도, 회사 IdP로 페더레이션할 때도 발급자는 STS 하나다 — Cognito는 “이 외부 사용자가 진짜인가”만 판정하고 발급은 기존 통로에 맡긴다. 그래서 감사·만료 같은 성질이 다른 경로와 똑같이 적용된다.",
      },
    },

    // ── 12 정책 변수 ────────────────────────────────────────────────────
    {
      id: "c15",
      section: "12",
      q: "버킷 하나·테이블 하나를 백만 명이 공유하면서 서로의 데이터를 못 보게 하는 방법을 설명해 보세요.",
      a: "정책 변수를 쓴다. 정책 안에서 ${cognito-identity.amazonaws.com:sub}로 그 사용자의 고유 ID를 참조하면, 정책 문서는 하나인데 실행 시점에 각자의 ID로 치환되어 사용자마다 다른 범위가 적용된다 — 사용자마다 버킷이나 정책을 만들 필요가 없다.",
      why: {
        q: "사용자마다 버킷을 따로 만드는 설계가 왜 답이 되지 못할까요?",
        a: "버킷·테이블 같은 리소스에는 계정 단위 한도가 있고, 사용자가 늘 때마다 리소스를 만들고 지우는 운영이 따라붙기 때문이다. 정책 변수는 리소스를 하나로 두고 접근 범위만 사용자별로 갈라서, 사용자 수가 늘어도 만들어야 할 것이 늘지 않는다.",
      },
    },
    {
      id: "c16",
      section: "12",
      q: "S3와 DynamoDB에서 사용자별 격리에 쓰는 조건 키를 각각 들고, DynamoDB 쪽이 무엇을 가리키는지 말해 보세요.",
      a: "S3는 s3:prefix — 자기 프리픽스 아래만 접근하게 한다. DynamoDB는 dynamodb:LeadingKeys이고, 이것은 파티션 키를 가리킨다. 파티션 키를 사용자 ID로 설계해 두면 “자기 파티션 키를 가진 항목만 읽고 쓴다”가 조건 한 줄로 성립한다.",
    },

    // ── 13 ALB 인증 오프로드 ────────────────────────────────────────────
    {
      id: "c17",
      section: "13",
      q: "API Gateway가 토큰에 하는 일과 ALB가 하는 일의 차이를 말해 보세요.",
      a: "API Gateway는 앱이 이미 받아 온 토큰을 검증한다. ALB는 한 걸음 더 나아가 로그인 자체를 대신 수행한다 — 미인증 요청을 Cognito Hosted UI로 리다이렉트해 로그인시키고 세션 쿠키를 발급한 뒤, 인증된 요청만 타깃 그룹으로 넘긴다. 그래서 백엔드에서 인증 코드를 통째로 걷어낼 수 있고, 이것을 인증 오프로드라고 부른다.\n전제 조건은 HTTPS 리스너다 — HTTP 리스너에는 인증 액션을 걸 수 없다.",
      why: {
        q: "인증 규칙이 HTTPS 리스너에서만 허용되는 이유는 무엇일까요?",
        a: "인증 과정에서 자격 증명과 세션 쿠키가 오가기 때문이다. HTTP는 평문이라 그 값이 그대로 노출되고, 쿠키를 가로채면 로그인 자체를 훔칠 수 있다 — 그래서 암호화된 리스너에서만 허용한다.",
      },
    },
    {
      id: "c18",
      section: "13",
      q: "미인증 요청을 어떻게 다룰지 정하는 설정과 그 세 값을 말해 보세요.",
      a: "OnUnauthenticatedRequest다. authenticate(기본값 — IdP 로그인 페이지로 리다이렉트), deny(요청 거부, HTTP 401 반환), allow(인증 없이 그대로 통과)의 세 값이 있다. 리스너 규칙은 경로별로 걸 수 있어서, 공개 화면은 allow, 개인 화면은 authenticate로 가를 수 있다.",
    },

    // ── 14 조합과 총정리 ────────────────────────────────────────────────
    {
      id: "c19",
      section: "14",
      q: "두 풀을 이어 쓰는 가장 완전한 형태를 그리고, 그것이 항상 필요한 것은 아닌 이유도 말해 보세요.",
      a: "① User Pool로 로그인시켜 ID 토큰을 받고 ② 그 ID 토큰을 Identity Pool의 Logins 맵에 넣어 STS 임시 AWS 자격 증명으로 바꾼 뒤 ③ S3·DynamoDB에 직접 접근한다 — “인증은 CUP, 인가는 CIP”다. Access·Refresh 토큰은 이 교환에 대신 쓰지 않는다.\n다만 백엔드 API만 보호하면 되는 앱은 User Pool 하나로 끝난다(API Gateway 조합). 사용자가 AWS 리소스를 직접 만져야 할 때 비로소 Identity Pool이 붙으므로, “둘 다 써야 한다”가 항상 정답인 것은 아니다.",
    },
  ],

  /**
   * 도식 재현 — CIP 자격 증명 발급 경로. 이 챕터에서 선형 체인이 정확히 들어맞는 흐름은
   * 이것 하나다 (CUP 로그인은 연합 로그인 가지가 갈라지고, ALB 흐름은 리다이렉트로 되돌아온다).
   */
  diagram: {
    prompt: "외부 사용자가 S3를 직접 호출하기까지 토큰과 자격 증명이 거치는 길을 순서대로 떠올려 보세요.",
    nodes: [
      { role: "사용자를 로그인시키고 토큰을 내주는 곳", name: "User Pool (또는 외부 IdP)" },
      { role: "받은 토큰이 진짜인지 검증하고 교환을 주선하는 곳", name: "Identity Pool" },
      { role: "IAM 역할을 맡아 임시 자격 증명을 실제로 발급하는 서비스", name: "STS" },
      { role: "임시 자격 증명으로 직접 호출되는 대상", name: "S3 · DynamoDB 등 AWS 리소스" },
    ],
    // 라벨·이름을 짧게 유지한다 — 이 리포에서 diagram 을 쓰는 첫 챕터라 렌더 폭이 실측된 적이
    // 없었는데, 긴 문구는 노드 상자와 화살표 라벨 칸을 넘겨 잘린다 (#272 프리뷰 실측).
    // 눈대중 상한: 노드 이름 24자, 엣지 라벨 15자.
    edges: [
      "① 로그인 성공 → JWT 발급",
      "② 토큰 전달 → 유효성 검증",
      "③ 역할을 맡아 자격 증명 발급",
    ],
  },

  mixed: [
    {
      id: "m1",
      scenario: "사내 개발자 30명에게 배포 파이프라인 접근 권한을 주려고 한다.",
      service: "IAM (Cognito 아님)",
      why: "내가 신뢰하는 내부 주체이고 규모가 수십 명이다 — IAM 사용자·그룹·역할이 다루는 영역이다.",
      contrast: "Cognito는 AWS 계정 밖의 앱 사용자용이다. 같은 문제에서 “앱 고객 수십만 명”으로 바뀌면 답이 Cognito로 뒤집힌다.",
    },
    {
      id: "m2",
      scenario: "모바일 앱 사용자가 자기 사진을 S3 버킷에 직접 업로드해야 한다. 사진은 사용자별로 격리돼야 한다.",
      service: "Identity Pool + 정책 변수",
      why: "“직접 접근”이 임시 AWS 자격 증명을 가리키고, 격리는 s3:prefix에 ${cognito-identity.amazonaws.com:sub}를 건 정책 변수로 푼다.",
      contrast: "User Pool만으로는 AWS 리소스를 직접 만질 수 없다 — User Pool이 주는 것은 JWT일 뿐 AWS 자격 증명이 아니다.",
    },
    {
      id: "m3",
      scenario: "EC2 인스턴스 위의 애플리케이션이 S3를 읽어야 한다. 액세스 키를 코드에 두고 싶지 않다.",
      service: "IAM 역할 (인스턴스 프로파일)",
      why: "AWS 계정 안의 워크로드가 주체다 — 역할을 붙이면 임시 자격 증명이 자동 공급된다.",
      contrast: "여기서 Cognito를 떠올리면 오답이다. 임시 자격 증명이 나온다는 점은 Identity Pool과 같지만, 주체가 외부 앱 사용자가 아니라 내 계정의 리소스다.",
    },
    {
      id: "m4",
      scenario: "서버리스 API를 사용자별로 보호해야 한다. 프런트엔드는 이미 로그인 토큰을 갖고 있다.",
      service: "API Gateway + Cognito 사용자 풀 권한 부여자",
      why: "토큰을 Authorization 헤더로 받아 검증하고, 통과한 요청만 Lambda로 넘긴다. REST API Cognito 권한 부여자에는 JWT 원문을 보낸다.",
      contrast: "ALB 인증은 로그인 자체를 대신 수행하는 쪽이다 — 이미 토큰이 있고 검증만 필요하면 API Gateway 권한 부여자가 맞다.",
    },
    {
      id: "m5",
      scenario: "EC2 위에서 도는 레거시 웹앱에 로그인을 붙여야 하는데, 애플리케이션 코드는 수정할 수 없다.",
      service: "ALB + authenticate-cognito",
      why: "ALB가 HTTPS 리스너에서 인증을 끝내고 인증된 요청만 백엔드로 보낸다 — 백엔드에는 인증 코드가 없다.",
      contrast: "API Gateway 권한 부여자는 앱이 토큰을 보내 준다는 전제가 있어 클라이언트 수정이 필요하다. “코드 수정 없이”가 ALB를 가리키는 신호다.",
    },
    {
      id: "m6",
      scenario: "가입이 확정된 직후 환영 메일을 보내고 사용자별 초기 데이터를 만들어야 한다.",
      service: "Post Confirmation Lambda 트리거",
      why: "가입 확인이 끝난 뒤 시점에 걸리는 후크이고, User Pool이 Lambda를 동기 호출한다.",
      contrast: "Pre Sign-up은 가입을 받아들이기 전이라 아직 사용자가 확정되지 않았고, Pre Token Generation은 토큰 클레임을 손보는 자리다.",
    },
  ],
};
