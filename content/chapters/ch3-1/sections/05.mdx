import { Code, ExamLi, ExamPoint, Note, Table, WarnBox } from "../../ui";
import { Term } from "../../interactive";

로그인 한 번에 토큰이 <b>세 개</b> 나옵니다. 셋을 “JWT 토큰”으로 뭉뚱그려 외우면 시험에서 갈리는 지점을 놓칩니다 — 용도가 다르고, 수명도 다릅니다.

<Table
  head={["토큰", "용도", "기본 수명", "설정 가능 범위"]}
  rows={[
    [
      <b>ID 토큰</b>,
      <>사용자가 <b>누구인지</b>를 담는다 — 이름·이메일·<Code>sub</Code> 같은 신원 클레임. 백엔드에 “이 사람이다”를 알린다</>,
      <>60분</>,
      <>5분 ~ 1일</>,
    ],
    [
      <b>Access 토큰</b>,
      <>사용자가 <b>무엇을 해도 되는지</b>를 담는다 — OAuth 2.0 스코프. API 호출을 인가하는 데 쓴다</>,
      <>60분</>,
      <>5분 ~ 1일</>,
    ],
    [
      <b>Refresh 토큰</b>,
      <>앞의 두 토큰을 <b>다시 발급받는</b> 데만 쓴다 — 사용자를 다시 로그인시키지 않기 위한 것</>,
      <>30일</>,
      <>60분 ~ 10년</>,
    ],
  ]}
/>

수명이 이렇게 갈리는 이유는 <b>탈취됐을 때의 피해</b> 때문입니다. ID·Access 토큰은 요청마다 네트워크에 실려 나가므로 새어 나갈 기회가 많고, 그래서 짧습니다. Refresh 토큰은 앱이 안전한 곳에 보관한 채 재발급 때만 쓰므로 노출이 적고, 그래서 길게 둡니다.

<WarnBox>ID·Access 토큰의 유효기간은 <b>Refresh 토큰의 유효기간을 넘길 수 없습니다</b>. Refresh를 60분으로 줄여 놓고 Access를 1일로 두는 설정은 성립하지 않습니다 — 재발급 수단이 먼저 죽는 조합이기 때문입니다.</WarnBox>

<Note>실제 화면에서는 이렇게 나타납니다 — 앱을 한 달 만에 열었는데 로그인 화면이 뜨지 않으면 Refresh 토큰이 살아 있어 조용히 재발급된 것이고, 로그인 화면이 뜨면 Refresh 토큰까지 만료된 것입니다.</Note>

<ExamPoint>
  <ExamLi>셋을 한 줄로 — <b>ID는 신원, Access는 권한, Refresh는 재발급</b>.</ExamLi>
  <ExamLi>수치는 <b>ID·Access 60분 / Refresh 30일</b>이 기본값이다. 이 두 숫자가 그대로 선택지에 나온다.</ExamLi>
  <ExamLi>“사용자를 자주 다시 로그인시키지 않으려면?” → <b>Refresh 토큰 유효기간</b>을 늘린다 (Access를 늘리는 선택지는 오답 — 보안만 나빠지고 재로그인 주기는 Refresh가 정한다).</ExamLi>
</ExamPoint>
