import { Code, CodeBlock, ExamLi, ExamPoint, Note, Table } from "../../ui";
import { Term } from "../../interactive";

받은 토큰을 어디에 쓰는지의 첫 번째 답이 <Term id="api-gateway">API Gateway</Term>입니다. DVA 서버리스 아키텍처 문제에 거의 고정으로 나오는 조합입니다 — <b>사용자 → API Gateway(토큰 검증) → Lambda → DynamoDB</b>.

앱은 토큰을 <b>HTTP 요청 헤더</b>에 실어 보냅니다. 이때 <Code>Authorization</Code> 헤더에 담기는 토큰을 <b>베어러 토큰(bearer token)</b>이라고 부릅니다 — “이 토큰을 가진(bear) 자에게 권한을 준다”는 뜻이고, 그래서 토큰 자체가 곧 열쇠입니다.

<CodeBlock title="앱이 보내는 요청">{`GET /orders HTTP/1.1\nHost: api.example.com\nAuthorization: Bearer eyJraWQiOiJ…`}</CodeBlock>

API Gateway는 이 토큰을 <b>Cognito 권한 부여자(Authorizer)</b>로 검증합니다. 서명이 유효한지, 만료되지 않았는지, 우리 User Pool이 발급한 것인지를 확인하고 — 통과해야만 뒤의 Lambda가 호출됩니다. 어느 토큰을 보내느냐에 따라 검사하는 것이 달라집니다.

<Table
  head={["보내는 토큰", "API Gateway가 하는 일"]}
  rows={[
    [
      <><b>Access 토큰</b></>,
      <>메서드에 <b>인가 스코프(Authorization scopes)</b>를 설정해 두면, 토큰의 OAuth 2.0 스코프가 그 메서드·경로에 맞는지 검사한다</>,
    ],
    [
      <><b>ID 토큰</b></>,
      <>유효하고 만료되지 않았으면 통과시키고, <b>토큰 내용을 백엔드로 넘긴다</b> — 백엔드가 사용자 정보를 그대로 받아 쓸 수 있다</>,
    ],
  ]}
/>

<Note>권한 부여자는 API 유형에 따라 이름이 다릅니다 — REST API는 <b>Cognito 사용자 풀 권한 부여자</b>, HTTP API는 <b>JWT 권한 부여자</b>가 내장돼 있습니다. 더 복잡한 판단(그룹·속성별 분기)이 필요하면 <b>Lambda 권한 부여자</b>로 직접 짭니다.</Note>

<ExamPoint>
  <ExamLi>토큰은 <b><Code>Authorization</Code> 헤더의 베어러 토큰</b>으로 전달된다 — 쿼리 문자열이나 바디가 아니다.</ExamLi>
  <ExamLi>“API를 사용자별로 보호” + “서버리스” → <b>API Gateway + Cognito 권한 부여자</b>가 기본 정답 형태다.</ExamLi>
  <ExamLi>스코프 기반으로 메서드를 가르려면 <b>Access 토큰</b>이다 — ID 토큰에는 스코프 개념이 없다.</ExamLi>
</ExamPoint>
