import type { SelfQuizEntry } from "../../schema";

/**
 * ch0-1 섹션 셀프 퀴즈 (이슈 #99) — 인출 카드(session.ts) 아래 자기채점 덱.
 * 문항 지침(#98): 짧은 시나리오/사실 큐 + 판정 가능한 1~2문장 정답. 인출 카드와
 * 주제 중복은 허용하되 문장 재탕 금지 — 카드 = 서술·정교화, 셀프 퀴즈 = 핵심 사실 판정.
 *
 * §00(왜 AI 시대에 AWS를 공부하는가)은 문항을 두지 않는다: 출제 대상이 아닌 동기 부여
 * 서문이라 판정할 시험 사실이 없다 — session.ts 의 §00 카드 제외(#54 결정)와 같은 논리.
 */
export const selfQuiz: SelfQuizEntry[] = [
  // ── 01 리전 / 가용영역(AZ) ─────────────────────────────────────────────
  {
    section: "01",
    q: "EC2 인스턴스 1대로 돌아가는 서비스가 있다. 그 인스턴스가 있는 AZ에 정전이 나면 서비스는 어떻게 되고, 시험 정답 기준의 예방책은?",
    a: "함께 멈춘다 — EC2 인스턴스는 특정 AZ 하나에 놓이는 리소스다. 예방책은 서로 다른 AZ에 2대 이상 나눠 배포(Multi-AZ) — 고가용성(HA) 문항의 기본 정답 패턴이다. 참고로 모든 리전은 AZ를 최소 3개 갖는다.",
  },
  {
    section: "01",
    q: "“EBS 볼륨을 다른 AZ의 EC2 인스턴스에 붙인다” — 이 구성이 성립하나? 성립 여부를 가르는 개념은?",
    a: "성립하지 않는다 — EBS 볼륨·EC2 인스턴스·서브넷은 AZ 단위 리소스라 같은 AZ여야 붙는다. 가르는 개념은 서비스의 “범위”: IAM·Route 53·CloudFront는 글로벌, S3·DynamoDB·Lambda는 리전, EC2·EBS는 AZ.",
  },
  {
    section: "01",
    q: "전 세계 사용자의 정적 콘텐츠 지연시간을 줄여야 한다 — 리전을 여러 개로 늘리는 안과 CloudFront 중 시험 정답 패턴은?",
    a: "CloudFront — 리전·AZ와 별개인 전 세계 수백 개 엣지 로케이션에 콘텐츠 사본을 캐시해 사용자 가까이에서 응답한다. 동적 서비스 전체의 지연이면 사용자 가까운 리전 선택이 답이 된다.",
  },

  // ── 02 AWS API의 구조 ──────────────────────────────────────────────────
  {
    section: "02",
    q: "“콘솔에서는 되는 작업인데 CLI로는 불가능할 수 있다” — 이 서술이 왜 틀렸나?",
    a: "콘솔·CLI·SDK는 전부 동일한 HTTPS API 엔드포인트로 서명된(SigV4) 요청을 보내는 다른 껍데기라, 가능한 작업의 목록도 IAM 권한 검사도 채널과 무관하게 같다.",
  },
  {
    section: "02",
    q: "환경변수에 액세스 키가 있고, EC2 인스턴스에는 IAM 롤도 붙어 있다 — SDK는 어느 자격증명을 쓰나?",
    a: "환경변수 — Credential Provider Chain은 코드 파라미터 → 환경변수 → 설정 파일 → 롤 순서로 탐색하며, 명시적 지정이 환경에 붙은 기본값(롤)을 이긴다.",
  },
  {
    section: "02",
    q: "API 호출이 403으로 실패했다. 에러 코드가 AccessDenied일 때와 SignatureDoesNotMatch일 때 각각 무엇을 의심해야 하나?",
    a: "AccessDenied는 인가 실패 — IAM 정책(권한)을 본다. SignatureDoesNotMatch는 서명·자격증명 문제 — 키 값·설정을 본다. 상태 코드(둘 다 403)가 아니라 에러 코드로 구분하는 것이 트러블슈팅형 문항의 단서다.",
  },

  // ── 03 요금의 기본 사고방식 ────────────────────────────────────────────
  {
    section: "03",
    q: "문제 지문에 “minimal operational overhead”·“least management effort”가 보인다 — 선택지 판단의 기본 방향은?",
    a: "관리형/서버리스 쪽 선택지에 가중치 — 프로비저닝·패치·백업·장애 조치를 AWS가 대신하는 방향이 DVA 정답 패턴의 압도적 다수다.",
  },
  {
    section: "03",
    q: "중단돼도 다시 이어 하면 되는 야간 배치 작업을 “가장 비용 효율적으로” 돌려야 한다 — EC2 구매 옵션 정답과 근거는?",
    a: "스팟 인스턴스 — 최대 90% 할인 대신 중단될 수 있어, 중단 허용 워크로드가 조건일 때의 정답이다. 장기 상시 워크로드라면 예약·Savings Plans.",
  },
  {
    section: "03",
    q: "과금 3축 중 데이터 전송 — 요금이 크게 붙는 방향은 어느 쪽이고, 반대 방향은?",
    a: "AWS 밖으로 나가는 아웃바운드에 붙는다. 들어오는 인바운드는 대부분 무료 — 과금 3축은 컴퓨팅(실행 시간)·스토리지(용량×기간)·데이터 전송(아웃바운드)이다.",
  },
];
