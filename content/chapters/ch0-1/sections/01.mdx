import { ChLink, Code, ExamLi, ExamPoint, Fig, Note, SubTitle, Table } from "../../ui";
import { GlobalInfraSvg } from "../figs";

{/* 텍스트를 품는 커스텀 컴포넌트(Note·ExamLi)는 한 줄로 쓴다 — 여러 줄이면 MDX가
    내용을 블록(문단)으로 파싱해 p 태그가 중첩된다. */}

AWS는 전 세계에 데이터센터를 깔아 두고, 이를 **리전(Region) → 가용영역(AZ, Availability Zone) → 데이터센터**의 계층으로 묶어서 제공합니다.

<Fig caption="계층 구조: 리전 ⊃ 가용영역(AZ) ⊃ 데이터센터. 엣지 로케이션은 이와 별개의 촘촘한 캐시 거점망.">
  <GlobalInfraSvg />
</Fig>

<SubTitle>핵심 개념</SubTitle>

<Table
  head={["용어", "정의", "기억할 것"]}
  rows={[
    [
      "리전 (Region)",
      <>
        지리적으로 독립된 지역. 예: 서울 <Code>ap-northeast-2</Code>
      </>,
      "리전을 선택하면 대부분의 리소스는 그 리전 안에만 존재. 기준: 지연시간, 규제(데이터 주권), 서비스 지원 여부, 가격",
    ],
    [
      "가용영역 (AZ)",
      "리전 내 물리적으로 분리된 데이터센터 그룹 (모든 리전은 최소 3개)",
      <>
        한 AZ가 정전·재해로 죽어도 다른 AZ는 살아있음 →{" "}
        <b>Multi-AZ = 고가용성(HA)의 기본기</b>
      </>,
    ],
    [
      "엣지 로케이션",
      "전 세계 수백 개의 캐시/접점 거점",
      "CloudFront, Route 53, Global Accelerator가 사용. 리전·AZ와는 별개",
    ],
  ]}
/>

<SubTitle>서비스의 “범위” 감각</SubTitle>

- **글로벌 서비스**: <ChLink id="ch0-2">IAM</ChLink>, Route 53, CloudFront — 리전을 골라도 전체에 적용
- **리전 서비스**: S3, DynamoDB, Lambda — 리전 단위로 존재 (대부분이 여기 해당)
- **AZ 단위 리소스**: EC2 인스턴스, EBS 볼륨, 서브넷 — 특정 AZ 하나에 놓임

<Note>EBS 볼륨은 같은 AZ의 EC2에만 붙일 수 있다 — 이런 식으로 “이 리소스는 어느 범위에 사는가”가 뒤 단계의 함정 문제로 계속 재활용됩니다.</Note>

<ExamPoint>
  <ExamLi>“리전이 무엇인가” 같은 직접 질문은 드물고, <b>고가용성을 위해 여러 AZ에 배포</b>하라는 선택지의 전제로 등장</ExamLi>
  <ExamLi>지연시간을 줄여야 한다 → 사용자와 가까운 <b>리전 선택</b> 또는 <b>CloudFront(엣지)</b>가 정답 패턴</ExamLi>
  <ExamLi>리소스 이름 규칙 감각: 리전 코드 <Code>ap-northeast-2</Code>, AZ 코드 <Code>ap-northeast-2a</Code></ExamLi>
</ExamPoint>
