import { ChLink, Code, ExamLi, ExamPoint, Note, SubTitle, Table } from "../../ui";
import { CodeBlock, WarnBox } from "../figs";

AWS에 접근하는 길은 세 가지 — **콘솔·CLI·SDK** — 이고, 각각 다른 자격증명을 씁니다. <ChLink id="ch0-1">ch0-1</ChLink>에서 본 대로 셋 다 결국 같은 API를 부릅니다.

<Table
  head={["접근 방법", "무엇", "자격증명"]}
  rows={[
    ["관리 콘솔", "웹 브라우저 UI", "비밀번호 + MFA"],
    [
      "CLI",
      <>
        터미널 명령줄 도구. 퍼블릭 API에 직접 접근, 스크립트 자동화·오픈 소스.{" "}
        <Code>aws configure</Code>로 키 등록
      </>,
      "액세스 키",
    ],
    [
      "SDK",
      "언어별 라이브러리 — 앱 코드 안에서 API 호출. JS·Python(boto3)·Java·Go 등 + 모바일·IoT SDK",
      "액세스 키",
    ],
  ]}
/>

<Note>CLI와 boto3(Python SDK)는 모두 저수준 라이브러리 <b>botocore</b> 위에 구축된 별개 도구입니다. 그리고 SDK에서 리전을 지정하지 않으면 기본값으로 <b>us-east-1</b>이 사용됩니다 — 상식형 포인트.</Note>

<SubTitle>자격증명 3종 정리</SubTitle>

<Table
  head={["종류", "구성", "특징"]}
  rows={[
    ["콘솔 로그인", "유저명 + 비밀번호 (+ MFA)", "사람 전용. 계정 단위 비밀번호 정책 적용"],
    [
      "액세스 키",
      <>
        <b>Access Key ID</b>(≈ 유저명) + <b>Secret Access Key</b>(≈ 비밀번호)
      </>,
      <>
        CLI/SDK용. 콘솔에서 직접 생성하며 <b>Secret은 생성 시점에만 확인 가능</b>. 공유·
        하드코딩 금지
      </>,
    ],
    [
      "임시 자격증명",
      <>
        키 + Secret + <b>SessionToken</b> + 만료
      </>,
      "STS 발급. 롤 기반 접근에 사용 — 자동 만료·갱신",
    ],
  ]}
/>

<Note>참고 — <b>CloudShell</b>: 콘솔 안에서 바로 열리는 무료 브라우저 터미널로, CLI가 사전 설치돼 있고 로그인한 유저의 자격증명을 자동 사용하며(액세스 키 설정 불필요) 리전별 1GB 영구 스토리지를 줍니다. 도구 생태계 전반은 <ChLink id="ch0-1">ch0-1</ChLink> 몫이라 여기서는 이 정도만 짚습니다.</Note>

<SubTitle>비밀번호 정책과 MFA</SubTitle>

계정 단위 **비밀번호 정책**으로 최소 길이, 문자 유형(대/소문자·숫자·특수문자), 자체 변경 허용, 만료(주기적 강제 변경), 이전 비밀번호 재사용 금지를 강제할 수 있습니다.

**MFA = 알고 있는 것(비밀번호) + 가지고 있는 것(디바이스).** 비밀번호가 유출돼도 물리 디바이스가 없으면 계정이 뚫리지 않습니다. 루트와 관리자급 유저에게는 필수로 권장됩니다.

<Table
  head={["MFA 디바이스", "예시", "특징"]}
  rows={[
    ["가상 MFA 디바이스", "Google Authenticator, Authy", "스마트폰 앱. 한 기기에 여러 토큰(여러 계정/유저) 지원"],
    ["U2F 보안 키", "YubiKey (서드파티)", "물리 USB 키. 하나의 키로 여러 루트/IAM 유저 지원"],
    ["하드웨어 키 팹", "Gemalto (서드파티)", "OTP를 표시하는 전용 물리 장치"],
    ["GovCloud용 키 팹", "SurePassID (서드파티)", "AWS GovCloud (US) 전용"],
  ]}
/>

<WarnBox>액세스 키를 코드·리포지토리·AMI에 넣는 선택지는 <b>언제나 오답</b>입니다. 키가 정말 필요한 곳(로컬 개발 머신 등)에서만 쓰고, 정기적으로 순환(rotate)합니다.</WarnBox>

<ExamPoint>
  <ExamLi>“앱 코드에서 AWS 호출” → <b>SDK</b>, “터미널/스크립트 자동화” → <b>CLI</b>, “브라우저에서 키 설정 없이 CLI” → <b>CloudShell</b></ExamLi>
  <ExamLi>Secret Access Key는 <b>생성 시 한 번만</b> 표시 — 분실하면 재발급</ExamLi>
  <ExamLi>MFA 디바이스 유형 매칭(가상/U2F/하드웨어) — 빈출도는 낮지만 나오면 그대로 득점</ExamLi>
</ExamPoint>
