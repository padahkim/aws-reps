import type { SelfQuizEntry } from "../../schema";

/**
 * ch0-2 섹션 셀프 퀴즈 (이슈 #98) — 인출 카드(session.ts) 아래 자기채점 덱.
 * 문항 지침(#98): 짧은 시나리오/사실 큐 + 판정 가능한 1~2문장 정답. 인출 카드와
 * 주제 중복은 허용하되 문장 재탕 금지 — 카드 = 서술·정교화, 셀프 퀴즈 = 핵심 사실 판정.
 *
 * §10(DVA 시험 핵심 정리)은 문항을 두지 않는다: 총정리 섹션 자체가 인출 페이지 성격이라
 * 덱이 이중 인출이 된다 — session.ts 의 §10 카드 제외와 같은 논리 (#74 히어링 결정 준용).
 */
export const selfQuiz: SelfQuizEntry[] = [
  // ── 01 IAM 개요 ────────────────────────────────────────────────────────
  {
    section: "01",
    q: "IAM 유저를 서울 리전 콘솔에서 만들었다. 도쿄 리전에서 이 유저로 작업하려면 무엇을 더 해야 하나?",
    a: "아무것도 필요 없다 — IAM은 글로벌 서비스라 리전 개념이 없고, 만든 유저·롤·정책은 모든 리전에서 그대로 적용된다. 리전을 묻는 선택지는 함정이다.",
  },
  {
    section: "01",
    q: "새 IAM 유저를 만들고 아무 정책도 붙이지 않았다 — 이 유저가 기본으로 할 수 있는 일은? IAM 사용 자체의 요금은?",
    a: "아무것도 못 한다 — 기본은 전부 거부이고, 필요한 권한만 명시적으로 부여한다(최소 권한 원칙 — “가장 적절한 권한 설계” 문항의 판정 기준). IAM 자체는 무료다.",
  },

  // ── 02 핵심 구성요소 ───────────────────────────────────────────────────
  {
    section: "02",
    q: "“개발팀 그룹을 전사 그룹 안에 넣어 계층으로 관리한다” — 이 설계가 성립하나?",
    a: "성립하지 않는다 — 그룹에는 유저만 들어가고 그룹 중첩은 불가다. 한 유저가 여러 그룹에 속하는 것은 가능하다.",
    yn: "아니오",
  },
  {
    section: "02",
    q: "관리자 권한이 매일 필요한 운영자가 루트 계정으로 일상 작업을 한다 — 시험 정답 기준의 올바른 구성은?",
    a: "루트는 MFA를 걸어 초기 설정 외 사용 금지하고, 일상 작업은 AdministratorAccess 정책을 단 IAM 유저(또는 롤)로 한다.",
  },

  // ── 03 인증 vs 인가 ────────────────────────────────────────────────────
  {
    section: "03",
    q: "유효한 액세스 키로 서명한 요청이 AccessDenied를 받았다 — 인증·인가 중 어느 단계의 실패이고, 어디를 점검해야 하나?",
    a: "인가 실패다 — 신원 확인(인증)은 통과했지만 정책 평가가 거부했다. 자격 증명 정책의 Allow만 보지 말고 관련 정책 층 전체 — 명시적 Deny·리소스 정책·Permission Boundary·SCP(§05·§06) — 를 점검한다. 서명 자체가 문제면 SignatureDoesNotMatch 같은 별도 에러가 난다.",
  },

  // ── 04 정책 JSON 해부 ──────────────────────────────────────────────────
  {
    section: "04",
    q: "정책의 Version을 “최신 날짜로 갱신해야 한다”는 선택지가 왜 함정인가?",
    a: "Version은 날짜가 아니라 고정 문자열 “2012-10-17”(정책 언어 버전)이다. 생략하면 구버전 언어로 해석돼 정책 변수 등이 동작하지 않는다.",
  },
  {
    section: "04",
    q: "유저 100명에게 각자의 S3 홈 폴더만 허용해야 한다 — 정책은 몇 개 필요한가?",
    a: "1개 — 정책 변수 ${aws:username}로 Resource 경로를 동적 매칭한다. DynamoDB에서 “자기 항목만”은 dynamodb:LeadingKeys 조건 키.",
  },
  {
    section: "04",
    q: "s3:GetObject를 Allow했는데 Resource가 arn:aws:s3:::my-bucket(버킷 ARN)뿐이라 객체 읽기가 거부된다 — 왜?",
    a: "객체는 arn:aws:s3:::my-bucket/* 라는 별도 ARN이다 — 버킷 ARN과 객체 ARN이 다르므로 GetObject의 Resource는 /* 쪽이어야 한다.",
  },

  // ── 05 정책 유형과 유효 권한 ───────────────────────────────────────────
  {
    section: "05",
    q: "Permission Boundary만 설정되어 있고 자격 증명 정책이 없는 유저 — 무엇을 할 수 있나? Boundary를 그룹에 걸 수는 있나?",
    a: "아무것도 못 한다 — Boundary는 상한만 정의할 뿐 권한을 부여하지 않는다 (유효 권한 = Identity ∩ Boundary). 그리고 Boundary는 유저·롤에만 걸 수 있고 그룹에는 불가다.",
  },
  {
    section: "05",
    q: "여러 유저가 같은 정책을 공유·재사용해야 한다 — 인라인 정책과 고객 관리형 정책 중 정답은?",
    a: "고객 관리형 — 독립적으로 존재해 여러 주체에 재사용되고 버전 관리·롤백을 지원한다. 인라인은 주체 하나에 1:1 전속이라 공유가 성립하지 않는다.",
  },

  // ── 06 정책 평가 로직 ──────────────────────────────────────────────────
  {
    section: "06",
    q: "IAM 정책에 아무 권한이 없는 유저가, 같은 계정 버킷 정책의 Allow만으로 그 버킷에 접근할 수 있나?",
    a: "가능하다 — 동일 계정에서는 자격 증명 정책과 리소스 정책이 합집합으로 평가된다. 교차 계정이라면 양쪽 모두 Allow가 필요하다. (4케이스 진리표의 최대 함정)",
    yn: "예",
  },
  {
    section: "06",
    q: "IAM 정책은 Allow, 버킷 정책에 명시적 Deny — 결과는?",
    a: "거부 — 명시적 Deny는 자격 증명·리소스 정책·SCP 어디에 있든 하나만 있어도 모든 Allow를 무효화한다.",
  },

  // ── 07 롤 & STS ───────────────────────────────────────────────────────
  {
    section: "07",
    q: "EC2 안의 앱이 S3를 호출해야 한다. 액세스 키를 인스턴스에 저장하는 안과 인스턴스 프로파일(롤) 중 정답과 근거는?",
    a: "롤 — STS가 발급한 임시 자격 증명이 자동 제공·순환되어 유출돼도 만료된다. 키를 코드·환경변수·AMI에 넣는 선택지는 언제나 오답이다.",
  },
  {
    section: "07",
    q: "사내 SAML SSO 사용자가 AWS 임시 자격 증명을 받는 STS API는? 웹/모바일 앱(OIDC) 로그인 사용자라면?",
    a: "사내 SSO는 AssumeRoleWithSAML, 웹/모바일(Cognito·Google 등 OIDC)은 AssumeRoleWithWebIdentity. 교차 계정 기본은 AssumeRole, MFA 자기발급은 GetSessionToken.",
  },
  {
    section: "07",
    q: "개발자에게 ec2:RunInstances 권한이 있는데 롤을 붙여 EC2를 시작하는 것만 실패한다 — 빠진 권한은?",
    a: "iam:PassRole — 서비스에 롤을 지정하는 작업자에게 필요하며, Resource는 전달할 롤 ARN으로 좁힌다. 전달 후에도 서비스가 못 쓰면 롤의 신뢰 정책 Principal을 본다.",
  },

  // ── 08 접근 방법과 자격 증명 ────────────────────────────────────────────
  {
    section: "08",
    q: "동료가 Secret Access Key를 분실했다 — 콘솔에서 다시 확인할 수 있나?",
    a: "없다 — Secret은 생성 시점에 한 번만 표시된다. 새 액세스 키를 발급하고 이전 키는 비활성화·삭제한다.",
    yn: "아니오",
  },
  {
    section: "08",
    q: "“SDK에서 리전을 지정하지 않으면 기본값 us-east-1이 쓰인다” — 맞는 서술인가?",
    a: "틀렸다 — 대부분의 SDK는 리전 미지정 시 에러를 낸다 (boto3는 NoRegionError). ‘기본 us-east-1’은 구식 강의 자료의 트리비아다.",
    yn: "아니오",
  },

  // ── 09 보안 도구·모범 사례·공동 책임 ───────────────────────────────────
  {
    section: "09",
    q: "① 계정 전체 유저의 MFA·키 로테이션 상태를 CSV로 뽑기 ② 특정 유저의 서비스별 마지막 접근 시각 보기 — 각각의 도구는?",
    a: "① 자격 증명 보고서(Credential Report, 계정 수준) ② 액세스 어드바이저(Access Advisor, 유저 수준 — 미사용 권한 제거용). 둘을 바꿔 묻는 스왑 함정이 최다 출제다.",
  },
  {
    section: "09",
    q: "다른 계정이나 퍼블릭에 의도치 않게 공유된 리소스를 찾아 주는 IAM 도구는? 배포 전에 정책을 시험해 보는 도구는?",
    a: "외부 공유 탐지는 IAM Access Analyzer, 배포 전 “이 요청이 허용되는가” 사전 테스트는 Policy Simulator.",
  },
];
