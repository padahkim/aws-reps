# PLAYBOOK.md — DVA 프로젝트 운영 매뉴얼 (v4)

> **지위**: 인간(나)이 보는 **운영 매뉴얼**이다 — 규칙·순서·이유를 담고, 거의 변하지 않는다. 실시간 상태(무엇이 어디까지 됐고 다음이 뭔지)는 이 문서가 아니라 **`BOARD.md`(현황판)** 가 단일 출처다. v3까지 있던 마스터 체크리스트의 체크박스는 BOARD로 이관되어 폐지되었다.
> **v4 변경점**: 운영 모델 명문화(3층 구조 + 운영 루프), 체크인형 관제 세션 규정(§7), 실시간 상태의 BOARD.md 이관, CURRICULUM v3.1(Next.js 확정) 반영.
> **v4.1 변경점**: **모의고사 모드 완전 제거 [인간, 2026-07-13]** 반영 — 단계 5와 델타에서 삭제. 하이브리드 근거는 퀴즈 채점·오답 노트·진도로 유지.
> **대원칙: 일하는 세션은 동시에 최대 2개(축2+축1 파이프라인 병렬)까지만. 트랙을 넘나드는 병렬은 금지.**

---

## 0. 운영 모델 — 3층 구조

| 층 | 파일/주체 | 성격 |
|---|---|---|
| 기준 | `docs/RUBRIC.md`, `docs/CURRICULUM.md` | 판단의 근거. 인간만 수정 |
| 지도 | `PLAYBOOK.md` (이 문서) | 규칙·순서·이유. 거의 불변 |
| 현황판 | `BOARD.md` | **유일하게 계속 변하는 파일.** 작업 상태·승인 대기함·세션 로그 |
| 손 | 세션들 (축2/축1/표준화/앱/생성/체크인) | 일한다. **세션끼리 직접 통신 불가 — 모든 커뮤니케이션은 파일+커밋 경유** |

관제는 "상주 세션"이 아니라 **체크인**이다(§7): 필요할 때 열어 상황 보고와 다음 지시문을 받고 닫는다. 상주형을 두지 않는 이유 — 세션 간 직통이 없어 관제도 결국 파일을 읽고 쓸 뿐이고, 오래 살아있는 세션은 컨텍스트가 쌓여 판단이 흐려지며, 승인 병목은 어차피 인간에게 있다.

## 1. 운영 루프 — 한 사이클을 도는 법

1. **BOARD.md를 본다** → 「승인 대기함」을 먼저 처리하고, 「작업 보드」에서 상태가 `대기`인 다음 작업과 담당 세션을 확인한다
2. **그 세션 하나를 연다** → 해당 시작 프롬프트를 투입하고, 아래 **워커 리추얼**을 함께 붙인다:
   > "시작 시 BOARD.md를 읽고 네 임무 행을 확인하라. 배치 완료·정지 지점마다 네 행의 상태와 산출물 경로를 갱신하고, 세션 로그에 한 줄을 append하라. **다른 행과 다른 세션의 산출물은 수정 금지.** 상태 어휘는 BOARD 머리말의 것만 사용하라."
3. **세션이 일한다**: 산출물 커밋 + BOARD 자기 행 갱신 + 정지 지점(캘리브레이션 등)에서 멈춰 「승인 대기함」에 항목을 올린다
4. **나는 승인만 한다**: 대기함 항목을 처리하고 BOARD에 결과를 기록한다 (인간 개입 지점은 §8)
5. 반복. **헷갈리거나 단계가 바뀌면 관제 체크인(§7)을 연다**

BOARD 충돌 방지: 워커는 자기 행 + 로그 append만 만진다(리포트 쓰기 경계와 같은 원리). 워크트리 병렬 시에도 행 단위 분리라 머지가 안전하다.

## 2. 자산 분류

### A. 기준·지도·현황 문서 (실행하지 않는다)
| 파일 | 역할 |
|---|---|
| `docs/CURRICULUM.md` (v3.1) | 도면: jsx 하이브리드 아키텍처, **Next.js(App Router)+TS 확정**, 챕터 모듈 규약(v0 가설·v1 확정란), 커리큘럼, 앱 요구사항 |
| `docs/RUBRIC.md` | 평가 기준·프로토콜 전문. 형식 무관 설계라 무수정. RUBRIC의 "스키마"·SCHEMA_FEEDBACK은 "규약·표현 형식"에 대한 것으로 해석 |
| `PLAYBOOK.md` | 이 문서 (운영 매뉴얼) |
| `BOARD.md` | 현황판 — 실시간 상태의 단일 출처 |

평가 산출물(자동 생성): `docs/EXAM_TASK_MAP.md`, `docs/VERIFIED_FACTS.md`, `docs/SCHEMA_FEEDBACK_AXIS2/AXIS1.md`, `reports/axis2·axis1/**`

### B. 세션 시작 프롬프트 (한 번에 하나, 리추얼과 함께 투입)
축2(실행 중) / 축1(축2 후 순차, 또는 파이프라인 병렬) / **규약 제안(단계 2-0 — 평가 산출물 종합, 프롬프트 준비됨: 세션프롬프트_규약제안)** / 표준화(규약 v1 후 — 그 시점에 프롬프트 요청) / 핸드오프=앱(표준화 후, §6 델타 반영) / 챕터 생성(규약 v1 기준으로 갱신 후, 신규 모드에서) / 관제 체크인(§7 — 수시)

### C. 폐기
루브릭 v1·정교화 / 축 단독 프로토콜 파일 / 커리큘럼 v1·v2 / PLAYBOOK v1~v3. 로컬에서 삭제 — 옛 기준("JSON 스키마가 목적지" 등)을 세션이 주워 읽는 사고 방지.

## 3. 트랙과 의존성 — 왜 이 순서인가

**결정 기록 [인간, 2026-07-13]**: ① 콘텐츠 영구 형식 = **jsx 하이브리드** — 본문은 jsx 자유 표현, `meta`·`quiz`는 구조화 export(CURRICULUM §3). 근거: jsx 표현력 유지 + 퀴즈 채점·오답 노트·진도에 필요한 기계 가독 문항 확보. ② 기술 스택 = **Next.js(App Router)+TypeScript**. ③ 릴리즈 전략 = **순차 릴리즈** — 릴리즈 1은 ch0-1·ch0-2·1-1(S3)·1-2(Lambda), 제작자 도그푸딩으로 학습 루프 가설을 검증한 뒤 릴리즈 단위로 확장. ※ v2의 "jsx 영구안 기각" 기록은 AI가 인간 결정 사항을 임의 확정한 오류였으므로 이 기록으로 대체.

```
[트랙 B: 평가] 축2 → 축1            의존: RUBRIC + /content의 jsx (형식 무관) + 형식 태깅 임무
      ↓
[인간] 형식 낙점 + 규약 v1 확정      의존: 형식별 점수 분포 + SCHEMA_FEEDBACK 2종 → CURRICULUM §3 갱신
      ↓
[표준화] /content 정비               의존: 규약 v1. meta·quiz export 부여, 문항을 공용 컴포넌트
      ↓                             데이터로, 본문은 낙점 형식으로 정돈(내용 수정 금지).
      ↓                             원본 jsx는 legacy/content-jsx/ 또는 브랜치 보존
[트랙 A: 앱] 핸드오프 → Next.js 셸   의존: 규약 v1 + 표준화된 /content.
      ↓                             앱 = 규약 export만 소비하는 셸(로더·네비·진도·공용 Quiz)
[트랙 C: 생성] 갭 챕터 생성           의존: 규약 v1 + EXAM_TASK_MAP·평가 결과 + 생성 프롬프트 갱신
```

핵심: **규약이 병목** — 규약 v1은 평가의 산출물이므로, 규약에 결합되는 작업(표준화·앱 셸·신규 생성)은 전부 평가 뒤로 간다.

## 4. 단계 지도 (계획의 원본 — 실시간 상태·체크는 BOARD가 단일 출처)

- **단계 1 · 평가**: 축2 점검·캘리브레이션 → 전수(판정 분포+형식 태그) → 축1 캘리브레이션 → 전수(형식별 점수 분포) → [인간] 재작성 표본 확인
- **단계 2 · 형식·규약 확정**: ⓪ **규약 제안 세션(2-0)** 이 평가 산출물(축2의 형식 태그 × 축1의 점수 결합, SCHEMA_FEEDBACK 2종)을 종합해 `docs/CONTRACT_PROPOSAL.md`(형식 후보·피드백 전수 처리표·v1 초안·결정 포인트)를 작성 → ① [인간] 제안서의 결정 포인트를 선택해 **형식 팔레트 확정**(단일 형식 강제가 아니라 형식 1~3개 + 챕터 성격별 적용 기준) + 규약 v1 확정 → ② CURRICULUM §3 갱신 (meta 이름·"use client" 위치 포함). **원칙: 인간이 원자료(수십 개 리포트)를 직접 종합하지 않는다 — AI가 종합·제안, 인간이 결정**
- **단계 3 · 표준화 (릴리즈 1 범위 우선)**: 생존 jsx 중 릴리즈 1 범위(ch0-1·ch0-2·1-1·1-2)부터 규약 적용(내용 수정 금지, 원본 legacy 보존), 결손 성분은 축1의 보충 생성 목록대로 별도 생성. 프롬프트는 이 시점에 요청
- **단계 4 · 앱**: 핸드오프(§6 델타) → 앱 코드 진단 → 리팩터링 vs 재작성 [인간] → Next.js 셸 마일스톤(챕터 로더 `app/chapters/[id]`+dynamic import → meta 네비+진도 → 공용 `<Quiz>`+오답노트) → 실데이터 검증
- **단계 5 · 신규 모드·릴리즈 루프**: RUBRIC 모드 「신규」 전환 [인간] → 생성 프롬프트를 "규약 v1 준수 jsx 모듈"로 갱신 → 릴리즈 1 잔여분 생성(신규 파이프라인 통과 필수) → **릴리즈 1 공개 → 도그푸딩 → 다음 릴리즈 생성**의 반복. 게이트 탈락은 2종 구분(RUBRIC §7-1): 품질 미달 = 재작성 / 규약 협소 = 개정 제안 + 콘텐츠 보류(`content-pending/`)

## 5. 지금 돌고 있는 축2 세션 — 투입할 지시 3줄 + 점검 3가지

지시 (그대로 붙여넣기):
1. "콘텐츠가 /content의 .jsx 파일 내부 텍스트다. 형식 무관 원칙대로 내부의 설명·퀴즈 텍스트를 평가 대상으로 삼고, React 컴포넌트 코드 자체는 평가 대상에서 제외하라."
2. "파일마다 사용된 표현 형식(예: 카드형 정리, 인터랙티브 퀴즈 내장, 아코디언, 벽글형, 비교표형)을 리포트에 한 줄로 태깅하라. 형식의 좋고 나쁨은 판정하지 마라 — 태깅만 한다."
3. §1의 워커 리추얼 (BOARD.md 읽기·자기 행 갱신·로그 append)

점검: 캘리브레이션(2~3개 후 정지) 지켰는가 / '검증됨'에 URL 붙고 VERIFIED_FACTS 쌓이는가 / reports/axis2 배치 커밋되는가. 순차 진행이면 워크트리 불필요 — 병렬 전환 시에만 RUBRIC §6.

## 6. 핸드오프 프롬프트 델타 — 실행 전 반영 (6가지)

1. 1차 임무에서 콘텐츠 진단 제거 → "콘텐츠 평가·표준화 완료 — reports/**와 BOARD.md 참조". **진단 범위는 앱 코드만**
2. 참조 데이터 모델 = "JSON 스키마"가 아니라 **확정된 챕터 모듈 규약 v1**(CURRICULUM §3)
3. 스택 = **Next.js(App Router)+TS 확정** — 진단 시 기존 코드가 다른 스택이면 마이그레이션 비용을 리팩터링 vs 재작성 판단에 포함하라
4. CLAUDE.md 생성 규칙 추가: "평가·검증 기준은 docs/RUBRIC.md. 신규 콘텐츠는 규약 v1 준수 + 신규 모드 파이프라인 통과 후에만 /content 진입. 상태 갱신은 BOARD.md"
5. 콘텐츠 이식 계획 교체: "표준화된 /content의 jsx 모듈을 그대로 로드" — 앱 세션은 콘텐츠를 만지지 않는다
6. 앱의 정체 = 본문 렌더러가 아니라 **규약 export만 소비하는 셸**(로더·네비·진도·공용 Quiz)

## 7. 관제 체크인 — 상주가 아니라 필요할 때 여는 짧은 세션

**여는 시점**: 단계 경계 / 승인 대기함이 쌓였을 때 / 뭔가 어긋난 느낌일 때 / 다음에 뭘 해야 할지 모르겠을 때
**체크인 프롬프트 (그대로 붙여넣기)**:
> 너는 이 프로젝트의 관제 체크인이다. 작업을 수행하지 마라. PLAYBOOK.md, BOARD.md, 그리고 reports/의 최신 리포트들을 읽고 다음만 하라: ① 현재 상황 3~5줄 보고 (어긋난 것·리스크 포함) ② BOARD.md 갱신안 제시 (내 승인 후 반영) ③ 다음에 열어야 할 세션과 거기 붙여넣을 지시문 초안 작성. 기준 문서(RUBRIC/CURRICULUM/PLAYBOOK) 수정은 제안만 하고 직접 하지 마라.

**하지 않는 것**: 평가·코드 작업 수행, 기준 문서 직접 수정, 장기 상주. 끝나면 닫는다 — 다음 체크인은 새 세션으로 연다(컨텍스트 누적 방지).

## 8. 인간 개입 지점 (이것만 내가 직접 한다 — 실시간 목록은 BOARD 승인 대기함)

축2/축1 캘리브레이션 승인 · 재작성 판정 표본 확인 · **우수 형식 낙점 + 규약 v1 확정 + CURRICULUM §3 갱신** · 리팩터링 vs 재작성 결정 · 마일스톤 승인 · RUBRIC 모드 전환 · BOARD 갱신안 승인. (완료된 결정: 콘텐츠 형식=jsx 하이브리드, 스택=Next.js — 2026-07-13)
